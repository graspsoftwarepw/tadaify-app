.DS_Store
.env
.env.local
.env.*.local
/node_modules/
*.tsbuildinfo

# React Router
/.react-router/
/build/

# Cloudflare Workers
.mf
.wrangler
.dev.vars*
!.dev.vars.example
worker-configuration.d.ts

# Supabase local volumes
supabase/.branches
supabase/.temp
supabase/storage
supabase/db-data
supabase/.env

# Playwright
playwright-report/
test-results/

# Editor
.idea/

# Cross-session hand-off note — per-machine local scratch, never committed (see global instructions).
.handoff/

# Isolated per-task git worktrees live inside the repo but are never tracked.
.worktrees/
