# tadaify MVP mockups — registry & changelog

> This folder is the **visual contract** for tadaify implementation. Every accepted
> mockup links to a GitHub Issue (F-XXX-NNN) which carries acceptance criteria,
> test plan, and DoD checklist. Implementation MUST match the mockup unless an
> approach-change comment is added to the issue per the orchestrator's
> `change-tracker` skill.

## Canonical mockups (final, ready for implementation)

| File | Purpose | Implementation story | Last accepted |
|------|---------|---------------------|---------------|
| `landing.html` | Public landing page — hero, social proof, pricing, FAQ | [#1 F-LANDING-001](https://github.com/graspsoftwarepw/tadaify-app/issues/1) | 2026-04-24 |
| `register.html` | Sign-up — 3-section progressive wizard, magic-link / Google OAuth | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `login.html` | Sister page to register, magic-link toggle, welcome-back hint via ?handle= | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `onboarding-welcome.html` | Step 1 — multi-select platform picker (9 platforms, mobile-first) | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `onboarding-social.html` | Step 2 — enter @handles for selected platforms (no OAuth, DEC-SOCIAL-01) | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `onboarding-template.html` | Step 3 — choose starter template (6 CSS-rendered previews) | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `onboarding-blocks.html` | Step 4 — confirm pre-populated social blocks + add manually | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `onboarding-tier.html` | Step 5 — pick plan (Free default, price-lock-for-life banner, universal $2 add-on) | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `onboarding-complete.html` | Success — confetti + clipboard copy + Motion v10 burst | [#3 F-FULLFLOW-001](https://github.com/graspsoftwarepw/tadaify-app/issues/3) | 2026-04-25 |
| `app-dashboard.html` | **Authenticated creator dashboard** — single sidebar + accordion + breadcrumb stepper for Design (v2 IA, accepted 2026-04-25) | [#26 F-APP-DASHBOARD-001](https://github.com/graspsoftwarepw/tadaify-app/issues/26) | 2026-04-25 |
| `app-domain.html` | Custom domain management — 3-step add wizard (Enter / DNS / Live) + Free-tier subscription overlay + status states | [#30 F-CUSTOM-DOMAIN-001](https://github.com/graspsoftwarepw/tadaify-app/issues/30) | 2026-04-25 |
| `app-settings.html` | **Authenticated creator settings** — Account / Billing / Security / GDPR & data / API keys (Pro) / Team (Business) / Danger zone (1-click cancel per AP-010, page-stays-live per AP-029, delete account per F-COMPLIANCE-001) | [#33 Account](https://github.com/graspsoftwarepw/tadaify-app/issues/33) · [#34 Billing](https://github.com/graspsoftwarepw/tadaify-app/issues/34) · [#35 Security](https://github.com/graspsoftwarepw/tadaify-app/issues/35) · [#36 GDPR](https://github.com/graspsoftwarepw/tadaify-app/issues/36) · [#37 API keys](https://github.com/graspsoftwarepw/tadaify-app/issues/37) · [#38 Team](https://github.com/graspsoftwarepw/tadaify-app/issues/38) · [#39 Danger zone](https://github.com/graspsoftwarepw/tadaify-app/issues/39) | 2026-04-25 |
| `app-settings-account.html` | **Settings → Account tab deep dive (creator-facing)** — `/app/settings` (Account tab). Standalone deep-dive of the existing `app-settings.html#account` stub. Breadcrumb (Home > Settings > Account) + tab nav cross-linking sister deep-dives (Billing / Security / GDPR & data) + tier-gated nav rows (API keys Pro · Team Business · Danger zone). **Identity section**: 140px circular avatar with hover-overlay + Upload / ✨ AI generate / Remove buttons routing to a CENTERED crop modal (NEVER drawer per `feedback_no_right_side_drawers`) with a circular crop mask, zoom slider, and toggleable AI generate panel; preview strip showing the avatar at 64/32/24/16 px so creators see how it looks in lists, comments, and the appbar. Display name (3–50 chars) with required-field validation + live "Visitors see: {name} · @{handle}" preview strip. **Username (handle)** input with `tadaify.com/` prefix, live availability check (current / too short / invalid / reserved / taken / expiring · Nd / available — 7 states), 30-day cooldown hint, Save handle button opening a centered confirm modal with old → new slug + DEC-074 redirect-target line resolved per tier (custom domain primary if any, else new tadaify slug). **Handle change history** (Pro+ — fully visible per `feedback_no_blur_premium_features`) as collapsible `<details>` with old → new + status chips (released / expiring), tier badge inline. Bio textarea (140 char limit with amber/red counter at 80/140) + ✨ Suggest button opening a 5-suggestion modal that swaps text in. Pronouns text input. **Location** with mocked city autocomplete (16-city DB, flag + region) — typing surfaces matches in a dropdown attached to the input. Personal website URL with https:// validation. **Date of birth** (Pro+ — fully visible) with Pro tier badge + privacy hint. **Email & notifications section**: primary email read-only display with Verified badge + "Change email →" button opening a centered modal (current email / new email / password confirm / Send verification link). **Recovery email** (Pro+ — fully visible) with empty default. Notification preferences: 4 toggle rows (Product updates, Weekly digest, Billing alerts, Security alerts) all on by default. **Profile visibility section**: Public profile toggle + "Show in tadaify discover" toggle (Creator+ — fully visible, off by default). **Sticky save bar** (fixed bottom, respects sidebar width) with Discard / Save changes; save handler builds features[] from `premiumDirty` Set and calls `window.TierGate.checkAndProceed` with the canonical Save / Save-without-premium / Upgrade / Cancel branches. Auto-save status flash on save. **Empty state** for brand-new user (state=empty in toolbar) — friendly emoji + "Welcome! Let's set up your profile" callout + auto-focus on Display name. Demo toolbar: tier (Free/Creator/Pro/Business) + state (filled/empty/unsaved) live. Sidebar canonical via shared partial. | [#33 F-APP-SETTINGS-ACCOUNT-001](https://github.com/graspsoftwarepw/tadaify-app/issues/33) | 2026-04-26 |
| `app-settings-security.html` | **Settings — Security tab deep dive (F-APP-SETTINGS-SECURITY-001)** — `/app/settings#security` deep-dive page. Sub-tab nav across Account / Billing / Security (active) / GDPR & data + cross-link to `app-settings.html`. Two-column layout: main column (Password section with auto-status strip + 90-day reminder + Sign-out-everywhere; 2FA section with Disabled/Enabled status pill + Setup-authenticator and Backup-codes management + Recovery-method note for ECN-02; Active sessions list with 5 sample devices including a 27-day stale Berlin row demonstrating the 90-day prune for ECN-05; Login history audit log with 12 sample rows + filter tabs (All / Successful / Failed) + free-text search + method pills (Password / Password+2FA / Magic link / Google OAuth) + CSV export; Connected accounts grid with Google connected + GitHub/Apple disconnect-able) and recommendations rail (live security score + 4 prioritised recs that auto-update with state). All editors are CENTERED MODALS per `feedback_no_right_side_drawers`: Change-password (current/new/confirm + zxcvbn-style 4-bar strength meter + live requirement checklist + show/hide eye + AC-2/3/4 inline error states); 3-step TOTP setup (decorative QR + base32 secret + Copy + ±30s clock-skew note for ECN-06 + 6-digit verify with `000000`-rejected demo for AC-6 + 10 backup codes shown once + Copy/Download/Print + ack checkbox before close + ECN-01 close-without-save protective prompt); View-remaining backup codes (8/10 unused with 2 masked); Regenerate confirm (ECN-08); Disable 2FA confirm (password + recovery email + ECN-09 cleanup note); Sign-out-all-others confirm (AC-11). Demo toolbar switches Tier (Free / Creator / Pro / Business — security identical across tiers per industry consensus, never paywalled per `feedback_no_blur_premium_features`) + State (filled / no-2fa / suspicious-detected — surfaces a danger banner with 3 failed sign-ins + auto-jump to Login history). Acceptance criteria 1–12 + edge cases 01–10 from issue body fully exercised in the mockup. | [#35 F-APP-SETTINGS-SECURITY-001](https://github.com/graspsoftwarepw/tadaify-app/issues/35) | 2026-04-26 |
| `app-settings-gdpr.html` | **GDPR & data settings — deep dive** of `app-settings.html#pane-gdpr`. Six sections: (1) Data export hero card + Recent exports table (3 sample rows, 7-day expiry, 5-download cap) + centered Request-export modal with 8 includable categories (profile / pages+blocks / subscribers / bookings / analytics / billing / audit / files) and ZIP+JSON+CSV format pill. (2) Cookie preferences for visitors — Essential (always-on, lists `tadaify_session` + `tadaify_csrf`) / Analytics default-on / Marketing default-off / Personalization default-off, each with retention metadata; Cookie banner preview stage with bottom-bar / corner-card / centered-modal (Pro+) style picker; cross-link to creator's Cookie Policy on `app-page-legal.html#cookie`. (3) Personal data on tadaify — 5 stat cards + expandable detailed breakdown (12 rows). (4) Policies you've accepted — ToS v3 / Privacy v2 / Cookie v2 / Subprocessor v3 (v4-available diff link) / AUP v1 with View-text + View-what-changed links. (5) **Pro+ tier-gated** DPA download + Subprocessor change notifications + current 4 active subprocessors (Stripe / Resend / Cloudflare / AWS) — section stays fully visible per `feedback_no_blur_premium_features`, gate fires at click via shared `TierGate.checkAndProceed`. (6) Erasure cross-link card → Danger zone tab (delete account is NOT here per story scope). Demo toolbar: tier switcher (free/creator/pro/business) + export-state switcher (default / pending / ready). Centered 720px modal for export request — never a right-side drawer per `feedback_no_right_side_drawers`. | [#36 F-APP-SETTINGS-GDPR-001](https://github.com/graspsoftwarepw/tadaify-app/issues/36) | 2026-04-26 |
| `app-settings-billing.html` | **Billing settings tab — deep dive (Stripe Portal hybrid per DEC-073)** — `/app/settings#billing`. Stand-alone deep-dive of the Billing pane with a 10-state demo toolbar (Free / Creator-active / Trial / Past-due / Cancel-scheduled / Card-expiring / Zero-invoices / EUR / Multi-sub-items / Business-yearly). Plan card with locked-for-life pill (DEC-PRICELOCK-01) + Active/Past-due/Cancel-scheduled chip + Change/Downgrade CTAs + monthly/yearly cadence segmented toggle (yearly = 10/12 of monthly, save-2-mo callout) + inline Compare-plans expander mirroring `pricing.html` (Free $0 / Creator $8 / Pro $19 / Business $49 — locked DEC-PRICELOCK-01 prices). Payment method card (mocked Visa **** 4242, exp 12/27, PCI-scope-0 trust strip) with Update-card → Stripe Portal redirect. Recent invoices read-only table (12 rows: subscription + domain add-ons + a refund credit + a failed retry; filter All/Paid/Open/Void/Failed; "Showing 12 of 47" pagination + "Open Stripe Portal for full history" CTA). Manage in Stripe section with three flow CTAs (`billing` / `payment_method_update` / `invoices`). Custom domain add-ons section showing $2/mo per verified domain (DEC-PRICELOCK-02) with renewal-failed inline error in past-due state. Cancel cross-link → Danger zone (canonical AP-010 path; portal cancel intentionally disabled per DEC-073) — also drives an in-page centered cancel-flow modal (3-step: reason radios + free-text textarea → confirm with AP-029 grace-period summary → done with reactivate option) for end-to-end review. Stripe Portal redirect modal with spinner + success state for card update. Change-plan modal with prorate calculation + locked-for-life reassurance. State banners: past-due (danger), card-expiring (warn), trial (info), cancel-scheduled (warn with Reactivate CTA). All modals are centered dialogs per `feedback_no_right_side_drawers`. Tab nav cross-links to sibling app-settings-<tab>.html files (Account / Security / GDPR / API / Team / Danger) on parallel worktrees. | [#34 F-APP-SETTINGS-BILLING-001](https://github.com/graspsoftwarepw/tadaify-app/issues/34) | 2026-04-26 |
| `admin-panel.html` | **Founder admin panel — `/admin`** — 8 sections (Overview / Users / Registration & waitlist / Maintenance / Moderation / Legal versioning / Health / Audit log). Distinct admin sidebar, role chip (Super-admin / Admin / Read-only), demo toolbar switches role for testing, all interactive controls do something. Integrates [#55 F-WAITLIST-001](https://github.com/graspsoftwarepw/tadaify-app/issues/55), [#5 F-LEGAL-002](https://github.com/graspsoftwarepw/tadaify-app/issues/5), [#6 F-LEGAL-003](https://github.com/graspsoftwarepw/tadaify-app/issues/6) and orchestrator standard Maintenance Mode. | [#58 F-ADMIN-PANEL-001](https://github.com/graspsoftwarepw/tadaify-app/issues/58) | 2026-04-26 |
| `app-insights.html` | **Insights tab** — single-handle Phase A scope, KPI tiles, cross-tab analyzer, time series, sources, blocks table, power-feature cards for Saved views (Pro+) / A/B (Business) / Replay (Business) / Identity stitching (Business) / Parquet R2 (Business) — fully visible previews per `feedback_no_blur_premium_features` (2026-04-26), tier-aware time-window picker | [#45 F-APP-INSIGHTS-001](https://github.com/graspsoftwarepw/tadaify-app/issues/45) | 2026-04-26 |
| `app-block-editor.html` | **Block editor centered modal** — 960px centered modal (full-screen on mobile) over frozen dashboard with two-column layout (form left, live preview right), 12 block-type forms, schedule visibility (Creator+) and A/B testing (Business) fully visible + interactive on every tier per `feedback_no_blur_premium_features` (2026-04-26) — gates at Save via the shared `TierGate` modal, AI suggest sub-modal, block-level analytics drill-down to Insights, type-to-confirm delete, duplicate, save/discard sticky footer. **A/B test redesigned 2026-04-26 (PR #54 extension):** Variant A \| B tabs in the Content section header split EVERY field of any block type into independent variants (label, url, icon, html, FAQ Q/A, social handles + per-platform icon styles, …); dual stacked preview + live diff summary + Copy A→B / Reset B→A. Plus an `A/B test available` / `active` identity chip in every Content section-head so the feature reads as named capability, not a footnote. **Social icons row redesigned 2026-04-26 (PR #54 extension):** empty default + `+ Add social` CTA → 30-platform picker → cards with drag-reorder + per-platform icon-style picker (6 base styles + 3 legacy alternates = 183 total synthesized variants). Drawer pattern rejected 2026-04-26 — see `feedback_no_right_side_drawers`. | [#52 F-APP-BLOCK-EDITOR-001](https://github.com/graspsoftwarepw/tadaify-app/issues/52) | 2026-04-26 |
| `app-block-picker.html` | **Block picker modal** — 720px modal with 3-column gallery of 12 block types, search, 7 category tabs (All / Links / Media / Forms / Shop / Layout / AI ✨), "Most clicked" badges, AI-suggest hero CTA → 5 ready-to-use block sets sub-modal, Pro+ tier badges with locked-state for lower tiers | [#53 F-APP-BLOCK-PICKER-001](https://github.com/graspsoftwarepw/tadaify-app/issues/53) | 2026-04-26 |
| `app-ai-suggest-modal.html` | **AI Suggest sub-modal** — 560px centered modal (full-screen on mobile) that opens from any ✨ Suggest button on title / label / caption / short-text fields. Sister of FIX-SUGGEST-RESTORE-001 (PR #70 round-2 polish that landed the trigger on every relevant field). Context strip (block-type summary + URL/alt-text), optional refinement textarea (visible empty per simple-UI rule, no Advanced toggle), 5 suggestion cards with style tags (Friendly / Direct / Playful / Professional / Curious), 6 documented states: Loaded / Loading (shimmer skeletons) / Error (friendly retry) / Empty (no input — Generate from context CTA) / Rate-limited (Free 30/day cap with tier upsell, typical creator never sees) / Selected confirmation. Standalone demo launcher with 3 trigger buttons (Block title / Caption / Button label) for cold review. Cost frame: Claude Haiku ~$0.0001/click, ~$50/year at 100k creators. | TBD — file F-AI-SUGGEST-MODAL-001 after acceptance | 2026-04-26 |
| `app-tier-gate-modal.html` | **TierGate modal — shared upsell flow at save** — 640px centered modal (full-screen on mobile), the SHARED upsell that appears when a creator clicks Save on a feature requiring a higher subscription tier. Called by every block editor + page editor + settings flow that touches a premium-tier field via `window.TierGate.checkAndProceed({ currentTier, features, onProceed, onSaveWithout, onUpgrade })`. Premium features stay fully visible + interactive everywhere per `feedback_no_blur_premium_features` (2026-04-26) — this modal is the ONLY upsell scene, gating ONLY at save. No sticky banners (per AP-031), no popups after N days, no email nags. Body is 4 sections: (1) "You set up:" trigger context (bulleted what creator did), (2) tier feature panel for the lowest-tier-that-covers-everything (Creator/Pro/Business with 5-7 features mirroring `pricing.html`), (3) "Your work is saved as draft" reassurance (load-bearing — without it, creator who walks away loses their config), (4) optional "Or save without `<feature>`" partial-save inline link. Friendly head: "Just one step — `<feature>` needs `<tier>`" (never "Upgrade required" / negative framing per `feedback_tadaify_simple_ui_no_advanced_toggle`). Sticky footer: Save as draft (left) + monthly/yearly toggle (Save 2 months) + Upgrade — `$X/mo` warm primary (right). Eight demo triggers cover every scenario: A/B testing (Free→Business), Custom domain (Free→Creator), Team invite (Pro→Business), Schedule beyond 7 days (Free→Creator), Klaviyo custom fields (Pro→Business), Multi-feature trigger, Already-on-right-tier (no modal — bottom-right toast), Admin-role required (workspace already on Business but action needs Admin role). Seven documented states swappable via state-strip chips inside the modal: Default / Loading checkout (spinner + disabled controls) / Stripe success (celebration + 12-square CSS confetti respecting `prefers-reduced-motion` + Continue→Publish CTA) / Stripe cancel (friendly retry/exit, no shame) / Yearly-plan creator (different upgrade label with "billed yearly · save $X" subtext) / Toast-only (no modal) / Admin-role pivot (different framing — "ask account owner"). Demo state machine in plain JS (no framework) — implementation PR lifts to a real component. | TBD — file F-TIER-GATE-MODAL-001 after acceptance | 2026-04-26 |
| `pricing.html` | Public pricing matrix — 4 tiers, compare table, Creator API spotlight, FAQ | [F-PRICING-LANDING-001 — TBD] | 2026-04-25 |
| `creator-public.html` | Public creator page — what visitors see at tadaify.com/handle | implicit (rendered via F-FULLFLOW-001 publish path) | 2026-04-24 |
| `product-public.html` | Public per-product page — tadaify.com/handle/p/slug | TBD (depends on F-PAGE-SHOP-001 #18) | 2026-04-24 |
| `app-page-blog.html` | **Blog page editor (creator-facing)** — `/app/pages/blog`. Page settings (title, slug, publish, RSS, page background, SEO expander), Layout config (cards/list/magazine, posts per page, sort, author byline), Posts table with filter tabs (All / Published / Drafts / Scheduled) + search + drag-reorder + kebab actions (Edit / Duplicate / Schedule / View public / Delete) + pagination. New/Edit post composer is a centered modal (NEVER a drawer per `feedback_no_right_side_drawers`) with cover image, AI-suggested title, slug, rich-text editor with formatting toolbar, tags multiselect, premium author dropdown (Business/Team — fully visible per `feedback_no_blur_premium_features`, gates at Save via shared `TierGate.checkAndProceed`), schedule datetime (Pro gate beyond 7 days), Save as draft / Schedule / Publish footer. Empty state with CTA + AI starter templates. Demo toolbar switches state (filled/empty) + tier (Free/Creator/Pro/Business). | [#19 F-PAGE-BLOG-001](https://github.com/graspsoftwarepw/tadaify-app/issues/19) | 2026-04-26 |
| `creator-blog-public.html` | **Public Blog render (visitor-facing)** — `tadaify.com/<handle>/blog`. Three sub-views in one file via in-page hash routing: list view (paginated 6-card grid + tag filter that actually filters), single post (cover + author/date/read-time + body + share + related posts + comments provider switcher Disqus/Hyvor/Off), tag-filter view. Inherits creator's theme (Indigo Serif, Crimson Pro display + Inter sans). Mobile-responsive: cards collapse to single column; pagination becomes "Load more". Footer mirrors `creator-public.html` social block. | [#19 F-PAGE-BLOG-001](https://github.com/graspsoftwarepw/tadaify-app/issues/19) | 2026-04-26 |
| `app-page-portfolio.html` | **Portfolio page editor (creator-facing)** — `/app/pages/portfolio`. Page settings (title, slug, publish, RSS, page background, SEO expander). Layout selector with 3 visual radios (Masonry / Grid / Carousel) + items-per-row slider + cover sizing dropdown + visitor filter chips toggle. Projects table with filter tabs (All 47 / Published 42 / Drafts 3 / Archived 2) + search + sort dropdown (Newest / Oldest / Manual / A→Z) + tag-filter chips that actually filter rows + drag-reorder + kebab actions (Edit / Duplicate / Mark/Unmark featured / Archive / View public / Delete) + pagination. Project rows show featured pin + video duration pill on covers, project metadata (year · medium · client). New/Edit project modal is a centered modal (NEVER a drawer per `feedback_no_right_side_drawers`) with cover media kind toggle (Image ↔ Video, per FIX-IMG-001 unified pattern), 8-image gallery with drag-reorder + remove, AI-suggested title, year picker, multi-select Medium/Category dropdown, rich-text description, tags, project URL, client, **Collaborators multi-select** (Business/Team — fully visible per `feedback_no_blur_premium_features`, gates at Save), **Featured toggle** (Free capped at 3, gates at Save for 4th+), schedule datetime (Pro gate beyond 7 days). Empty state with starter templates (Photographer / Designer / Architect / Illustrator). Demo toolbar swaps state (filled/empty) + tier (Free/Creator/Pro/Business). | [#16 F-PAGE-PORTFOLIO-001](https://github.com/graspsoftwarepw/tadaify-app/issues/16) | 2026-04-26 |
| `creator-portfolio-public.html` | **Public Portfolio render (visitor-facing)** — `tadaify.com/<handle>/portfolio`. Five sub-views in one file via in-page hash routing: Masonry list (default, CSS columns), uniform Grid, Carousel (hero stage + arrows + dots + thumbnail strip), single project (full-width cover + meta + Crimson Pro body + image gallery with centered-modal lightbox + collaborators block + external CTA + related), tag-filter view. Layout switcher in the page hero swaps grids live without reload; tag chips filter both grids in place. Inherits creator's theme. Mobile-responsive: Masonry collapses 3→2→1 columns; Carousel arrows + thumbs reflow; pagination becomes "Load more". Footer mirrors `creator-public.html` social block. | [#16 F-PAGE-PORTFOLIO-001](https://github.com/graspsoftwarepw/tadaify-app/issues/16) | 2026-04-26 |
| `app-page-about.html` | **About page editor (creator-facing)** — `/app/pages/about`. Page settings (title, slug, publish, show-in-nav, theme background, per-page custom domain Pro+, expandable SEO panel with ✨ Suggest). Page content = stack of section cards (Hero / Bio / Highlights / Story timeline / Press · Creator+ / Contact CTA), each draggable, collapsible, removable. Per-section editors: Hero (photo + name + tagline + social proof), Bio (rich-text editor + word counter + ✨ AI paragraph), Highlights (3–6 stat cards with icon picker), Timeline (year + title + desc + style picker list/cards), Press (quote cards with attribution), Contact CTA (label + target + button-style picker Pro+). Add-section centered modal with 6 type tiles + 3 starter templates (Musician / Author / Coach). Free tier capped at 4 sections; gated at Save via shared `TierGate.checkAndProceed`. Sticky save bar with auto-save status. Demo toolbar swaps state (filled/empty) + tier (Free/Creator/Pro/Business). | [#15 F-PAGE-ABOUT-001](https://github.com/graspsoftwarepw/tadaify-app/issues/15) | 2026-04-26 |
| `creator-about-public.html` | **Public About render (visitor-facing)** — `tadaify.com/<handle>/about`. Hero (big circular photo + name + tagline + social proof line), long-form Bio in Crimson Pro display font, Highlights grid (3-column responsive), Story timeline (alternating left/right markers on desktop, single-column on mobile), Press carousel (3 cards, dot indicators + auto-advance with hover-pause; horizontal scroll-snap on mobile), Contact CTA button, footer mirroring `creator-public.html` social block. Inherits creator's theme (Indigo Serif). Reduced-motion safe. | [#15 F-PAGE-ABOUT-001](https://github.com/graspsoftwarepw/tadaify-app/issues/15) | 2026-04-26 |
| `app-page-newsletter-signup.html` | **Newsletter signup page editor (creator-facing)** — `/app/pages/subscribe`. Dedicated full-page subscribe landing (Substack/Beehiiv style — different from the inline Newsletter signup BLOCK shipped in PR #74). Page-level controls (title, slug `/subscribe`, publish, theme color picker, expandable SEO panel, custom domain Pro+, A/B test Business — all fully visible per `feedback_no_blur_premium_features`). 8 reorderable / removable / collapsible sections: Hero (headline + subhead + cover + 4 ✨-Suggest-able templates) · Email provider (REUSES `BLOCK_TYPES.newsletter` exactly — 7 dropdown options Kit/Beehiiv/MailerLite/Mailchimp/Klaviyo/Google Sheets/Generic webhook per `feedback_tadaify_newsletter_mvp_scope_locked_2026_04_26`, NO Substack, NO Buttondown, Beehiiv DOI ON default, Klaviyo capture-name ON, webhook fallback `tadaify.com/wh/v1/<handle>` with copy button) · Signup form (one-line / two-line / centered-card layout picker, GDPR consent default ON with EU-compliance hint, customisable placeholder + button label + consent copy) · Social proof (subscriber count badge with live `{count}` template + provider pull, OR testimonial quotes, OR both; public-count visibility Creator+ above 2,500) · What you'll get (drag-reorder bullets with emoji + 4 starter templates: Weekly notes / Monthly digest / Drop alerts / Behind-the-scenes) · Past issues preview (Creator+ — 3 most recent posts pulled from provider archive) · FAQ (3-5 Q&A pairs pre-filled tackling common objections — frequency / unsubscribe / GDPR) · Footer CTA (closing button + social fallback links pulled from main page). Section management is centered-modal section picker per `feedback_no_right_side_drawers` (NEVER a drawer). Save handler carries `TierGate.checkAndProceed` placeholder. Demo toolbar swaps State / Provider / Tier live. | [#22 F-PAGE-NEWSLETTER-001](https://github.com/graspsoftwarepw/tadaify-app/issues/22) | 2026-04-26 |
| `creator-newsletter-signup-public.html` | **Public Newsletter signup page render (visitor-facing)** — `tadaify.com/<handle>/subscribe`. Single, focused conversion page (NOT a multi-section browse). All 3 form layouts (One-line / Two-line / Centered card) rendered as anchor sections (`#layout-oneline` / `#layout-twoline` / `#layout-card`) so reviewers can compare without leaving the file — in production only the chosen layout renders. All 5 confirmation states rendered as anchor sections via in-page hash routing: `#/` default, `#/submitting` (spinner — auto-advances to success after 1.5s for the demo), `#/success` ("Thanks! Check your inbox to confirm" + add-to-contacts CTA + resend link), `#/already-subscribed` ("You're already on the list — manage on Kit"), `#/error` (friendly retry + email creator + error code). Provider trust strip ("Powered by Kit · GDPR-friendly · 1-click unsubscribe"). Inherits creator's theme. Mobile-responsive: form columns stack, past-issues 1-col, hero scales. Footer mirrors `creator-public.html` social block. | [#22 F-PAGE-NEWSLETTER-001](https://github.com/graspsoftwarepw/tadaify-app/issues/22) | 2026-04-26 |
| `app-page-contact.html` | **Contact page editor (creator-facing)** — `/app/pages/contact`. Page settings (title, slug `/contact`, publish, theme color picker, expandable SEO panel with ✨ Suggest, custom domain Pro+ — fully visible per `feedback_no_blur_premium_features`, gated at Save). Inbox preview banner ("Inbox: 23 new messages") linking to dedicated `app-inbox.html` (TBD). 7 reorderable / removable / collapsible sections: Hero (headline + subhead + cover + 4 ✨-Suggest-able templates) · **Contact form** (visual field builder with drag-reorder rows, expand-to-config UI per row, GDPR consent locked at bottom) backed by a centered-modal **field-type picker** with 12 tiles (Short text / Long text / Email / Phone / URL / Number / Dropdown / Multiselect checkboxes / Radio choice / File upload Pro+ / Date picker / Hidden field Business+ for UTM tracking) + per-field config (label, placeholder, help text, required toggle, conditional visibility Pro+) + submit-button label with ✨ Suggest + after-submit dropdown (inline / redirect URL / custom Thank-you sub-page) + spam protection toggle (invisible reCAPTCHA + always-on honeypot) · **Where messages go** (Email forwarding default = creator email, multi-recipient Business+; Save to tadaify inbox toggle; 5 direct providers Pro+ as toggleable cards — Slack / Discord / Notion / Make.com / Generic webhook with inline endpoint config) + inbound endpoint `tadaify.com/wh/v1/<handle>/contact` with copy button (mirrors newsletter pattern from PR #74) · **Office hours** Creator+ (display-mode dropdown schedule/response-line/both, 7-day grid with per-day on/off toggle + open/close time inputs, timezone + typical-response-time selectors) · **Other contact methods** (public business email, phone, WhatsApp, Telegram, social handles auto-pulled from main page if blank, physical address, **map embed** toggle with live preview using Embedded-block oEmbed pattern — Google Maps iframe by default) · **FAQ-quickref** Creator+ (3-5 Q&A drag-reorder rows, cuts down repeat inquiries) · **Thank-you sub-page editor** as a centered modal (headline + body + drag-reorder related links). Add-section is a centered-modal picker per `feedback_no_right_side_drawers` with 8 tiles (5 already on page marked, 3 addable: Booking calendar Pro+ / Testimonials / future). Sticky save bar with auto-save status + Discard + Save → inline `TierGate.checkAndProceed` placeholder enumerating gated features (custom_domain, office_hours, faq_quickref, multi_recipient, webhook_provider:*, field:file_upload, field:hidden, field:conditional). Demo toolbar swaps State (filled/empty) + Tier (Free/Creator/Pro/Business). | [#21 F-PAGE-CONTACT-001](https://github.com/graspsoftwarepw/tadaify-app/issues/21) | 2026-04-26 |
| `creator-contact-public.html` | **Public Contact page render (visitor-facing)** — `tadaify.com/<handle>/contact`. Single focused inquiry-capture page. Mockup-only top strip with back-link + URL pill + theme toggle. Creator identity row (avatar + name + handle + "usually replies within 24h"). Hero (cover icon + clamp-sized headline + lede). **Office-hours strip** (Creator+, "Replies usually within 24h · Mon–Fri 9-17 GMT"). **FAQ-quickref accordion** (Creator+, 3 most-common questions before form to deflect repeats). **Form** with 5 visible fields (Name + Email required + Subject + Inquiry-type dropdown + Message required) + invisible **honeypot** (`hp-website` field, screen-reader-only via `clip:rect(0 0 0 0)`) + **GDPR consent** required + submit button "Send message" + spam-meta line. Required-field validation surfaces inline `.has-error` border on submit. **Other contact methods grid** — Email, Phone, WhatsApp, Instagram-DMs cards + 2-column **address-card** with mock map embed (CSS-only Google-Maps tile gradient + pin) + inline social-pill row (Instagram / X / LinkedIn). Footer mirrors `creator-public.html` social block. **5 visitor-facing states via hash routing** so reviewers see every path: `#/` default form · `#/submitting` (disabled spinner card, auto-advances to `#/success` after 1.5s) · `#/success` (custom thank-you message + 2 related-link CTAs from creator + "Send another" / "Back to main" buttons + message ID code) · `#/error` (friendly retry + email-creator fallback + error code) · `#/honeypot-caught` (mockup-only visible — explains the silent-fail spam path; in production a bot would silently get the success screen, see the comment in markup). Theme inheritance from `tokens.css`. Mobile-responsive: form columns stack at 540px, address-card map drops below text, social pills wrap. | [#21 F-PAGE-CONTACT-001](https://github.com/graspsoftwarepw/tadaify-app/issues/21) | 2026-04-26 |
| `app-page-faq.html` | **FAQ / Help Center page editor (creator-facing)** — `/app/pages/faq`. Distinct from the FAQ accordion BLOCK shipped in PR #70 round-2 polish (block = small accordion you drop into ANY page; this is the full PAGE template). Page settings (title with 4 quick-pick chips FAQ/Help Center/Support/Common questions, slug, publish, show-in-nav, theme color picker, expandable SEO panel). FAQ behaviour panel (visitor search toggle, TOC sidebar toggle, default-expand visual radio with First-open / All-collapsed / All-expanded, "Was this helpful?" feedback Creator+, cross-section tags Pro+, mocked search index preview). Sections + Q&A composer with editor-side search + collapse-all/expand-all + drag-reorder sections + drag-reorder questions inside a section + collapsible section cards + optional intro paragraph per section + add-question-inline button per section + AI suggest-5 CTA Creator+ + CSV import Pro+. Per-question helpful% pill on rows. New/Edit Q&A is a centered modal (NEVER a drawer per `feedback_no_right_side_drawers`) with section selector, question input (no ✨ Suggest — short text), full rich-text answer editor with ✨ Suggest answer (long-form text per FIX-SUGGEST-RESTORE-001), tags multiselect (Pro+ — fully visible per `feedback_no_blur_premium_features`, gates at Save). Add-section centered modal with 9 section-type tiles (Account / Billing / Shipping / Returns / Product / Technical / Booking / Legal / Blank) + 4 ✨ AI starter packs (E-commerce / Course / Coaching / General). CSV import centered modal with sample.csv link. Empty state with 4 starter templates + AI bootstrap link. Sticky save bar at bottom with auto-save status. Demo toolbar swaps state (filled/empty) + tier (Free/Creator/Pro/Business) live. Save handler runs `TierGate.checkAndProceed` collecting helpful-feedback (Creator) + cross-section-tags (Pro). | [#62 F-PAGE-FAQ-001](https://github.com/graspsoftwarepw/tadaify-app/issues/62) | 2026-04-26 |
| `creator-faq-public.html` | **Public FAQ / Help Center render (visitor-facing)** — `tadaify.com/<handle>/faq`. Five sub-views in one file via in-page hash routing: `#/` full FAQ default, `#/section/<slug>` single section deep-link (auto-expands all questions in section + scrolls), `#/q/<slug>` single question deep-link (auto-expands + scrolls + indigo target ring), `#/search?q=<query>` search results view (filters live across all sections, highlights matched terms, hides empty sections, no-match block with Contact CTA), `#/tag/<slug>` tag filter view (Pro+ public chip bar across all sections — fully rendered in this mockup so reviewers see the filtered state). Sticky search bar with backdrop blur. TOC sidebar (sticky desktop, collapsible mobile, scroll-spy highlights current section). Q&A items use native `<details>`/`<summary>` for keyboard + screen-reader baseline. Per-answer "Was this helpful?" Yes/No buttons (replace prompt with thanks). Per-answer permalink copy (clipboard fallback). Tag chips inside answers also trigger filter. "Still have questions?" footer CTA → links to Contact page. Footer mirrors `creator-public.html` social block. Theme inheritance via `tokens.css`. Mobile-responsive: TOC collapses to top-of-page accordion; search stays sticky; section spacing tightens. Mockup-only sub-view picker pill (bottom-left) lets reviewers click each route without typing hashes. | [#62 F-PAGE-FAQ-001](https://github.com/graspsoftwarepw/tadaify-app/issues/62) | 2026-04-26 |
| `app-page-links-archive.html` | **Links archive page editor (creator-facing)** — `/app/pages/links`. Browse-all-links sub-page for creators with 50+ links. Page settings (title with 4 quick-pick chips All links / Browse all / Links archive / The full catalogue + ✨ Suggest more, slug `/links`, publish toggle, show-in-homepage-footer toggle, 8-swatch theme color picker, expandable SEO panel). **Where do the links come from?** section is the load-bearing unique part — 5 source-page toggles (Homepage default ON · Blog post bodies · About page sections · All custom pages · Manually-added bonus links) each with per-source link count + live-count summary card ("143 links from 4 sources" with Recount-now action). **Manual bonus links** section reuses BLOCK_TYPES.linkButton primitive — drag-reorder list with inline expandable editor (title + URL + icon emoji/upload + A/B test variant Business + schedule visibility Creator+, all fully visible per `feedback_no_blur_premium_features`, gates at Save). **How visitors browse** section: 5 visual grouping-mode radios (Auto by source page default · Manual categories Creator+ · Alphabetical · Most clicked Creator+ · Newest first) + 4 visitor-control toggles (Search bar free · Tag filter chips Creator+ · Sort dropdown Pro+ · Public click counts Pro+) — each premium row carries an inline `chip` tier badge + `tier-hint` strip. **Excluded from archive** section lists 5 hidden links with reason field + Restore button; reason editor is a centered modal (NEVER drawer per `feedback_no_right_side_drawers`). Empty state ("No links to archive yet" friendly illustration + CTAs back to homepage editor and add manual link). Sticky save bar with auto-save status; save handler builds features[] from premiumDirty Set and calls `window.TierGate.checkAndProceed` collecting grouping-by-categories (Creator) + grouping-most-clicked (Creator) — exactly matches the pattern locked in PR #54 + PR #75. Demo toolbar swaps state (filled/empty) + tier (Free/Creator/Pro/Business) live. Sidebar canonical per `feedback_sidebar_pages_grouping`: shared partial mounts Pages parent (accordion-expanded) and the editor JS post-injects "Links archive" sub-item above the disabled "+ Add page" so it highlights as the active sub-page. | [#23 F-PAGE-LINKS-ARCHIVE-001](https://github.com/graspsoftwarepw/tadaify-app/issues/23) | 2026-04-26 |
| `creator-links-archive-public.html` | **Public Links archive render (visitor-facing)** — `tadaify.com/<handle>/links`. Searchable, group-able, filterable catalogue of every link button across the creator's site. Mockup-only top strip with back-link + URL pill + theme toggle. Creator identity row (avatar + name + handle + subtitle). Page hero with title + clamp-sized count badge ("143 links") + lede. **Sticky search + sort bar** with backdrop blur (search input has clear-× button that appears on type; sort dropdown swaps to A→Z / Most clicked / Newest / Group-by-source default — re-orders rows visually within each group). **Tag chip bar** below renders 9 sample tags + an "All" reset chip with per-tag counts (Creator+ feature; rendered fully in this mockup so reviewers see it). **4 sub-views via in-page hash routing**: `#/` full archive default (4 source-grouped sections rendered: From homepage 87 · From blog posts 42 · From About page 9 · Bonus archive-only 5), `#/group/<slug>` single-group deep-link (other groups hidden + smooth-scroll into view + results-meta strip with Show-all-groups link), `#/tag/<slug>` tag filter (filters across all groups via `data-tags` attribute, hides empty groups + results-meta strip with Clear-filter link), `#/search?q=<query>` live client-side search (case-insensitive substring on title + URL with matched-term `<span class="hi-match">` highlighting in indigo, hides empty groups, results-meta strip + no-match block with friendly Contact CTA). Each link row is a `BLOCK_TYPES.linkButton`-style card: 44px gradient icon tile · title + URL preview text + clickable tag chips · click-count badge (Pro+) · arrow on hover. Pagination = "Load more" button (~30 per page, bumps to 60/90/etc. up to 143). Footer mirrors `creator-public.html` social block (back-to-main pill + Instagram/TikTok/YouTube/Spotify/Email circular icon row + made-with `tada!ify` tag). Theme inheritance via `tokens.css`. Mobile-responsive: link list collapses 2-col → 1-col; sticky search persists; tag chips wrap; sort stays accessible. Mockup-only sub-view picker pill (bottom-left) lets reviewers click each route without typing hashes; disappears in production. | [#23 F-PAGE-LINKS-ARCHIVE-001](https://github.com/graspsoftwarepw/tadaify-app/issues/23) | 2026-04-26 |
| `app-page-schedule.html` | **Schedule / Booking page editor (creator-facing)** — `/app/pages/schedule`. For coaches, consultants, freelancers, instructors who take bookings. Page settings (title `Book a session`, slug `/book`, publish, theme color picker, expandable SEO panel). **Calendar integration** is the primary functional setup — 4-provider picker (Google Calendar / Apple Calendar / Outlook · all flagged post-MVP per DEC-APIPAGES-01 = C, OR Manual / Tadaify-only · recommended for MVP) + OAuth status panel (Connected as alex@gmail.com · Reconnect / Disconnect) + per-calendar Read-from / Write-to dropdowns + buffer (0/15/30/60 min) + booking-window (7/30/60/90 days) + auto-detected timezone + 7-row working-hours grid (per-day toggle + start/end time inputs). **Booking types** list (drag-reorder + filter tabs All/Active/Paused + per-type kebab Edit / Duplicate / Copy direct link / View public / Pause / Delete) capped at 2 active for Free with upgrade prompt; type composer is a centered modal (NEVER a drawer per `feedback_no_right_side_drawers`) with title + ✨ Suggest, optional cover image, duration (15-120 min + custom), Free/Paid toggle that gates at Save (Creator+ + Stripe Connect), price + per-type cancellation override, buffer override, visibility (Public / Hidden direct-link), full **booking form fields builder** (Name + Email always required + add Phone / Topic / Budget / etc. with field-type dropdown matching Contact page), description, post-booking thank-you, Active toggle. **Availability rules** (Creator+) — recurring rules (no Mondays / lunch break) + one-off blackouts (vacations) + month-grid visualizer (busy/free/blocked legend). **Notifications** — creator email (default ON) + day-before reminder (Creator+) + Slack/Discord/Notion/webhook chips (Pro+) for creator; visitor confirmation email with editable subject + body templates ({{name}} {{date}} {{cancel_link}}) + 24h reminder (Creator+) + 1h reminder (Pro+). **Payments** — Stripe Connect status panel (Active · payouts to alex@... · daily) + currency picker + default cancellation refund policy + deposit toggle. Empty state with 4 starter templates (Free intro / Paid 1-on-1 / Project consultation / Group workshop). Demo toolbar swaps state (filled/empty) + tier (Free/Creator/Pro/Business). | [#20 F-PAGE-SCHEDULE-001](https://github.com/graspsoftwarepw/tadaify-app/issues/20) | 2026-04-26 |
| `creator-schedule-public.html` | **Public Schedule / Booking render (visitor-facing)** — `tadaify.com/<handle>/book`. Identity row (creator avatar + handle + bio) + 3-step indicator. **Sub-views via in-page hash routing**: `#/` Step 1 — pick a session (4 booking-type cards with cover + duration + price + description + Pick-a-time CTA), `#/type/<slug>` Step 2 — pick a time (recap chip + month-grid calendar with busy/blocked/today states + timezone selector + Find-next-available CTA + 8-slot picker for selected day; mobile collapses to horizontal month-strip with scroll-snap), `#/type/<slug>/time/<iso>` Step 3 — fill details (booking form with Name/Email/Topic/optional Phone + Stripe Elements pretend-block for paid types + cancellation policy preview + side recap panel), `#/booked` Confirmation (🎉 + booking summary card + Download .ics + Add to Google Calendar buttons + Reschedule/Cancel links), `#/error` Friendly retry (Slot no longer available · card not charged · Pick another time CTA), `#/cancel/<token>` Cancellation page (returns from email link · refund preview · Reschedule-instead alternative + danger-styled Confirm), `#/reschedule/<token>` Reschedule page (current booking summary + 3 quick-pick day cards + Open full calendar fallback). Inherits creator's theme (Indigo Serif). Mobile-responsive: 2-panel layouts collapse to single-column; calendar grid swaps to month-strip below 480px. Footer mirrors `creator-public.html` social block. | [#20 F-PAGE-SCHEDULE-001](https://github.com/graspsoftwarepw/tadaify-app/issues/20) | 2026-04-26 |
| `app-page-custom.html` | **Custom page editor (creator-facing)** — `/app/pages/<slug>`. The most flexible page template — creator builds from scratch using the block library directly. NO opinionated section structure (unlike Blog/About/Portfolio/Newsletter/FAQ/Schedule which prescribe sections); just page settings + a stack of any of the 12 BLOCK_TYPES. Page header carries inline-editable title + URL pill + `Use template` + `Preview` buttons. Three section panels: (1) **Page settings** — slug with reserved-slug collision warning (blog/about/portfolio/subscribe/contact/faq/book/admin), Publish toggle, Show-in-nav toggle, Hide-from-search-engines toggle (`noindex`), 8-swatch theme color picker, expandable SEO panel (meta title/description with ✨ Suggest, auto-OG-image toggle). (2) **Page options** — power features fully visible per `feedback_no_blur_premium_features` with inline tier badges + tier hints: per-page **custom domain** override (Pro), **password protection** + password input (Pro), **redirect-after-N-seconds** (Pro). (3) **Page content** — drag-reorderable block list (8 sample blocks demonstrate Heading × 3, Image, Link × 5, Accordion, Newsletter — covering all 12 BLOCK_TYPES via the picker modal). Inline `+ Add block here` CTA between rows + `+ Add another block` at bottom + sticky `Add block` in section head. Per-block kebab menu: Edit / Duplicate / Schedule visibility (Creator+) / A/B test variants (Business) / Hide from public / Delete. Block flags inline (A/B active counter, Scheduled date range, Hidden). **Add-block modal** = 12-card centered modal (re-uses pattern from `app-block-picker.html`); Custom HTML card carries Business badge + triggers TierGate immediately on add. **Starter templates modal** = 6 ready-made compositions (Press kit · Speaking · Resources · FAQ-style · Coming soon · Thank you page) each with a `tc-blocks` chip strip showing the block sequence the template installs. Empty state is identical 6-template grid as friendly cards + primary CTAs. Sticky save bar with auto-save status; `trySave()` runs `TierGate.checkAndProceed` collecting only the actually-changed premium options (custom-domain / password / redirect). Demo toolbar swaps state (filled/empty) + tier (Free/Creator/Pro/Business) live. | [#14 F-PAGE-CUSTOM-001](https://github.com/graspsoftwarepw/tadaify-app/issues/14) | 2026-04-26 |
| `creator-custom-public.html` | **Public Custom page render (visitor-facing)** — `tadaify.com/<handle>/<slug>` (slug is creator-defined, e.g. `/press-kit`, `/speaking`, `/resources`). Three sub-views in one file via in-page hash routing: `#/` full page (creator nav with current page highlighted + page hero with title + sub-text + 8-block stack rendering Heading / Image / Link buttons / Accordion Q&A / Newsletter signup / Warm CTA link — same `tokens.css` palette as `creator-public.html`), `#/password` Pro+ password gate (centered lock screen with emoji + headline + password input + Unlock button + "Forgot? Contact creator" link; mockup hint shows working password `atelier-2026` matching the editor; success routes to `#/`), `#/blocks-empty` friendly placeholder for a published-but-empty page (🚧 emoji + "This page is being built" + 4 link chips back to other pages). Theme inheritance via `tokens.css`. Mobile-responsive: nav links wrap, blocks stack tighter, padding shrinks. Footer mirrors `creator-public.html` social block. Mockup-only top strip (back link + URL pill + sub-view picker pill + dark-mode toggle) disappears in production. | [#14 F-PAGE-CUSTOM-001](https://github.com/graspsoftwarepw/tadaify-app/issues/14) | 2026-04-26 |
| `app-page-legal.html` | **Legal page editor (creator-facing)** — `/app/pages/legal`. Multi-policy single page hosting Terms / Privacy / Cookie / Refund / DMCA / Accessibility (Pro+) / AUP (Business+) / Imprint / Custom — drag-reorder, per-policy version + effective date + last-updated, full rich-text body editor with auto-generated TOC preview, per-policy version history (Creator+, 7-year retention) with archived-version-view links, kebab actions (Copy public link / Duplicate / Export PDF / Remove). **Compliance helper sidebar** (right column on 1280+, auto-detects what creator needs based on Stripe products / EU traffic / cookie usage / contact-form attachments → ✓ met / ! needed / · n/a). **Templates picker centered modal** with 6 vetted starter templates (Generic ToS / Privacy GDPR / Privacy CCPA / Cookie / Refund Digital / DMCA Counter-notice) + prominent "templates are starting points, not legal advice — have a lawyer review" disclaimer. **Add-policy centered modal** with 9 type tiles (5 already-added shown disabled, 4 addable: Accessibility / AUP / Imprint / Custom). **Annual review reminder** with one-click "mark reviewed today + bump minor version on each". **Translation list** Pro+ (English ✓ + Polski / Deutsch / Español "Add" rows that route to TierGate). **Cookie banner integration link** to `app-settings.html#cookie-banner`. SEO defaults `noindex,follow` (recommended for legal pages). All starter content marked "Mockup placeholder text — replace before publishing. Have a lawyer review." Sticky save bar with `TierGate.checkAndProceed` collecting DMCA (Creator) + version retention (Creator) + multi-language (Pro). Demo toolbar swaps state (filled/empty) + tier (Free/Creator/Pro/Business). Sister of admin Legal versioning (`admin-panel.html` F-LEGAL-002 #5 / F-LEGAL-003 #6) — distinct concept (admin = founder-side tadaify ToS / Privacy, this = creator's own public Legal page). | [#17 F-PAGE-LEGAL-001](https://github.com/graspsoftwarepw/tadaify-app/issues/17) | 2026-04-26 |
| `creator-legal-public.html` | **Public Legal page render (visitor-facing)** — `tadaify.com/<handle>/legal`. Compliance-readable design — high-contrast neutral background overrides creator gradients, Crimson Pro display body at 17px / line-height 1.75, generous whitespace. Top policy-chooser pill bar (📜 Terms / 🔒 Privacy / 🍪 Cookie / 💸 Refund / ©️ DMCA — scroll-spy highlights current). Sticky TOC sidebar (240px on 960+, collapsible accordion on mobile) with section headers + per-policy sub-anchors (e.g. `#privacy-rights`) for deep-link sharing on ad platform privacy URL fields. Per-policy header carries version chip + effective-date badge + permalink-copy + single-policy mode buttons. Per-policy footer collapsible "View N past versions" with archived-version links. **Sub-views via hash routing**: `#/` full Legal page · `#/policy/<slug>` single-policy focused reading mode (other policies hidden) · `#/version/<slug>/<version>` archived version with persistent amber "you're viewing an archived version — view current →" banner · `#/print` print-optimized layout (TOC + nav + actions hidden, links print as `text (url)`, page breaks between policies). "Print this page" + "Copy page link" toolbar buttons. Mockup-only sub-view picker (bottom-left) lets reviewers click each route. `<meta name="robots" content="noindex,follow">` per legal-page best practice. Inherits creator's theme via `tokens.css`. Mobile-responsive: TOC collapses to top accordion; chooser pills wrap; policy headers stack. Footer mirrors `creator-public.html`. | [#17 F-PAGE-LEGAL-001](https://github.com/graspsoftwarepw/tadaify-app/issues/17) | 2026-04-26 |
| `try.html` | Guest-mode editor — pre-signup flow per DEC-SYN-PAT-067 | TBD | 2026-04-24 |

## Reference / exploration

| File | Purpose |
|------|---------|
| `app-dashboard-variants.html` | 10-layout exploration — V1 Classic Sidebar selected (preserved for documentation) |
| `index.html` | Hub linking to all mockups (review tool, not user-facing) |

## Shared assets

- `shared/tokens.css` — Indigo Serif palette + typography + spacing tokens
- `shared/partials.js` — nav + footer + **app-sidebar** partials. Mark a host element with `<div data-partial="app-sidebar" data-active="..." data-tier="..." data-handle="..." data-username="..."></div>` and the canonical Pages-parent sidebar is injected (CSS shipped inline). `data-active` accepts `pages|design|domain|insights|shop|settings|help`. Public marketing nav + footer use the same `data-partial="nav"` / `"footer"` markers as before.
- `shared/tokens.js` — Motion v10 logo SVG injection + theme bootstrap

## Sidebar consistency audit (2026-04-26)

Scope: every dashboard-style mockup must render the canonical sidebar — Pages parent (Home + disabled Add page per DEC-MULTIPAGE-01) → Design / Domain / Insights / Shop / Settings / Help & docs.

| Mockup | Active tab | Approach | Notes |
|--------|-----------|----------|-------|
| `app-dashboard.html` | `pages` (default) | **Inline** (preserved) | Owns the Pages-accordion + Design-accordion + tab-switching state machine; cannot be safely externalised. Cross-link inconsistencies fixed in this PR: Domain link no longer says "soon" (app-domain.html exists), Insights link points to `app-insights.html` (was an internal placeholder tab). |
| `app-domain.html`    | `domain`   | **Shared partial** | Migrated. |
| `app-insights.html`  | `insights` | **Shared partial** | Migrated. The partial keeps `id="side-tier"` on the `.uhandle` div so the existing tier-switcher JS keeps working. |
| `app-settings.html`  | `settings` | **Shared partial** | Migrated. |

### Cross-link inventory (post-migration)

| From → To | Status |
|-----------|--------|
| any sidebar → Home (`./app-dashboard.html?tab=page`) | ✅ |
| any sidebar → Design (`./app-dashboard.html?tab=design`) | ✅ |
| any sidebar → Domain (`./app-domain.html`) | ✅ |
| any sidebar → Insights (`./app-insights.html`) | ✅ |
| any sidebar → Shop (`./app-shop.html`) | ⏳ TODO — file doesn't exist; partial currently shows alert placeholder. Tracked as future work. |
| any sidebar → Settings (`./app-settings.html`) | ✅ |
| any sidebar → Help & docs (`./app-help.html`) | ⏳ TODO — file doesn't exist; partial shows alert placeholder. Tracked as future work. |
| `app-dashboard.html` block click → `app-block-editor.html` | ✅ (mockup demo flow) |
| `app-dashboard.html` "+ Add block" → `app-block-picker.html` | ✅ (mockup demo flow) |
| `app-block-picker.html` card click → `app-block-editor.html?type=<id>` | ✅ |
| `app-block-editor.html` analytics tile → `app-insights.html` | ✅ |
| `app-block-editor.html` "Add block" → `app-block-picker.html` | ✅ |
| `app-settings.html` Billing tab → "Custom domain add-ons" → `app-domain.html` | ✅ (existing) |

## Brand lock (every mockup MUST honour)

- Wordmark `tada!ify` (3-span: `ta` indigo / `da!` warm / `ify` foreground) — NO hyphen separator (DEC-WORDMARK-01)
- Tagline DEC-019: "Turn your bio link into your best first impression."
- Palette: Indigo Serif — `#6366F1` primary, `#F59E0B` warm, `#0B0F1E` dark canvas, `#F8F9FC` bg
- Fonts: Crimson Pro (display) + Inter (body)
- Motion v10 logo via `<span class="logo-mark" data-logo></span>`
- Light/dark mode toggle in topbar (consistent across all dashboard surfaces)
- Trust trio chip strip on auth pages: 🔒 Price locked for life · 💸 30-day money-back · 🛡 GDPR-compliant

## Locked DECs that gate implementation

| DEC | Decision | Where applied |
|-----|----------|---------------|
| DEC-WORDMARK-01 | `tada!ify` no hyphen | Every mockup |
| DEC-019 | Tagline locked | Hero + footer copy |
| DEC-PRICELOCK-01 | Price locked for life on active subs | pricing / onboarding-tier / dashboard / register / login |
| DEC-PRICELOCK-02 | Universal $2/mo custom-domain add-on | pricing / onboarding-tier / app-domain |
| DEC-MULTIPAGE-01 (B) | Multi-page post-MVP Q+1 — Free 1 / Creator 5 / Pro 20 / Business unlimited | dashboard sidebar (Add page disabled) / pricing / onboarding-tier |
| DEC-LAYOUT-01 (A) | Grid layout in MVP | F-LAYOUT-001 #9 + dashboard preview |
| DEC-APIPAGES-01 (C) | Reject OAuth integrations | DECs page templates / onboarding-social handle-only |
| DEC-CREATOR-API-01 (A) | Pro tier REST API + MCP server | pricing Pro card spotlight |
| DEC-AI-QUOTA-LADDER-01 (B) | Free 5 / Creator 20 / Pro 100 / Business unlimited | pricing / dashboard AI matcher / onboarding-tier |
| DEC-AI-FEATURES-ROADMAP-01 (A) | Text-only AI (theme matcher / bio rewrite / copy suggest) | dashboard Theme panel + future story |
| DEC-ANIMATIONS-SPLIT-01 (A) | Animations sub-tab = Entrance + Ambient | dashboard Design > Animations |
| DEC-WALLPAPER-ANIM-01 (C) | Wallpaper stays static; motion in Animations only | dashboard Design > Background |
| DEC-PINNED-MSG-01 (A) | Toggleable fading line above profile card | dashboard Page tab |
| DEC-CUSTOM-DOMAIN-NAV-01 (A) | Domain in main sidebar (was 6th item; now in Pages section) | dashboard sidebar |
| DEC-CUSTOM-DOMAIN-PROVIDER-01 (A) | Cloudflare for SaaS (Custom Hostnames API), Business plan | F-CUSTOM-DOMAIN-001 + app-domain |
| DEC-CUSTOM-DOMAIN-VALIDATION-01 (B) | TXT+CNAME fallback | app-domain step 2 DNS instructions |
| DEC-OPT-BADGE | Opt-in support badge default OFF, every tier free, no tracking | dashboard Design > Footer |
| DEC-SOCIAL-01 (B) | Handle-input only, no OAuth at signup | onboarding-welcome + onboarding-social |
| DEC-TRIAL-01 | No trials; 30-day money-back instead | pricing / onboarding-tier / register / login |
| DEC-TADAIFY-BILLING-01 (A) | Stripe for tadaify SaaS subscriptions | pricing checkout + onboarding-tier paid path |

## Anti-patterns enforced (verified by grep across all mockups)

- AP-001: NO "Powered by tadaify" forced — opt-in support badge only
- AP-013: NO phone field at signup — explicit "We never ask" microcopy
- AP-017: NO trials — 30-day money-back framing instead
- AP-024: Progressive reveal in register flow (3-section wizard, no monolithic form)
- AP-028: Pro/Creator gating shown as inline pills, never blocking modals
- AP-030: Free default on tier picker
- AP-031: NO sticky upgrade banner anywhere
- AP-045: Progressive disclosure on Add Block modal (6 default categories, "More" reveals full set)
- **AP-NO-BLUR-PREMIUM (2026-04-26, locked)**: NO blur, no `display:none` swap, no overlay-card-covering-feature for tier-gated UIs. Premium features are ALWAYS fully visible and interactive; gating happens at save/apply/click time via the shared `TierGate.checkAndProceed` modal. Required signals: inline `.tdf-tier-badge` in section header + (where applicable) `.tier-hint` / `.lc-cta-footer` / `.tier-banner` underneath. See `~/.claude/projects/-Users-waserekmacstudio-git-claude-project-orchestrator/memory/feedback_no_blur_premium_features.md` for the full rule.
- Zero competitor name-drops (Linktree / Beacons / Stan / Carrd / Bio.link / Taplink — verified by grep)

## Changelog

### 2026-04-26 — Account settings tab deep mockup (F-APP-SETTINGS-ACCOUNT-001 #33)

`mockups/tadaify-mvp/app-settings-account.html` (NEW). Standalone deep dive of the Account tab inside `/app/settings`. Builds on the existing `app-settings.html#account` stub by giving every identity field its own first-class treatment: 140px circular avatar with hover-overlay + Upload / ✨ AI generate / Remove buttons routed through a CENTERED crop modal (`feedback_no_right_side_drawers`) with circular crop mask + zoom slider + toggleable AI-generate panel; 4-size avatar preview strip (64/32/24/16 px) so creators see how it lands in lists, comments, and the appbar. Display name with required-field validation + live "Visitors see: {name} · @{handle}" preview line. Username (handle) input with `tadaify.com/` prefix and the canonical 7-state availability check (current / too short / invalid / reserved / taken / expiring · Nd / available) preserving `checkHandle` + `openHandleConfirm` naming for design continuity; Save handle confirm modal carries DEC-074 redirect-target line that resolves per tier (custom-domain primary if any, else new tadaify slug). **Handle change history** (Pro+ — fully visible per `feedback_no_blur_premium_features`) as a collapsible `<details>` with old → new + status chips. Bio textarea (140 char limit, amber/red counter at 80/140) + ✨ Suggest button opening a 5-suggestion modal that swaps text in. Pronouns. **Location** with mocked city autocomplete (16-city DB, flag + region) — typing surfaces matches in a dropdown anchored to the input. Personal website URL with https:// validation. **Date of birth** (Pro+ — fully visible) with privacy hint. Email & notifications: primary email with Verified badge + Change email modal (current / new / password); **Recovery email** (Pro+ — fully visible); 4 notification preference toggles (Product updates, Weekly digest, Billing alerts, Security alerts). Profile visibility: Public profile toggle + **Show in tadaify discover** (Creator+ — fully visible). Sticky save bar (fixed bottom, respects sidebar offset) wires `TierGate.checkAndProceed` from a `premiumDirty` Set with Save / Save-without-premium / Upgrade / Cancel branches; auto-save status flash on save. Empty state for brand-new users (state=empty in toolbar): friendly emoji + "Welcome! Let's set up your profile" callout + auto-focus on Display name. Demo toolbar: tier (Free/Creator/Pro/Business) + state (filled/empty/unsaved) live. Tab nav at top of settings content cross-links sister deep-dives (`app-settings-billing.html`, `app-settings-security.html`, `app-settings-gdpr.html`) plus tier-gated nav rows for API keys / Team / Danger zone (links into `app-settings.html` anchors). Sidebar canonical via shared partial.

Files: `mockups/tadaify-mvp/app-settings-account.html` (NEW), `mockups/tadaify-mvp/index.html` (Settings deep-dive section + card added), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table row).
### 2026-04-26 — GDPR & data settings tab deep mockup (F-APP-SETTINGS-GDPR-001 #36)

`app-settings-gdpr.html` (NEW) — full deep dive of the GDPR & data tab on `/app/settings`. Sister of in-flight `app-settings-account.html` / `app-settings-billing.html` / `app-settings-security.html`. Covers GDPR Art. 20 (data portability) + creator-side cookie controls + DPA/Art. 28 (Pro+) + Art. 17 erasure cross-link. Account deletion stays in the Danger zone tab — this tab only links to it.

- **Top-of-page export-ready notification** — green bar with download CTA, surfaces when an export is fresh (toggled by demo toolbar `Ready` state).
- **Section 1 — Data export hero** — `icon-bubble` + tagline ("You own your data.") + ZIP/JSON/CSV explainer + primary `Request export` CTA + 1/hour rate-limit hint. Below it the `Recent exports` table: 3 sample rows (Ready 18.4 MB / Expired 17.9 MB / Expired 15.2 MB) with status pill (`pill-status.ready` / `.pending` / `.expired`), expiry copy ("in 6d 23h · 4/5 downloads left"), per-row Download button. Footer note explains 7-day / 5-download retention.
- **Section 2 — Cookie preferences** — 4 `cookie-row`s: Essential (always-on, disabled toggle, lists `tadaify_session` + `tadaify_csrf` with retention via `cookie-meta-list code`), Analytics (default-on, lists `td_pv` + `td_clk`), Marketing (default-off, lists `_fbp` + `_gcl_au`), Personalization (default-off, lists `td_pref` + `td_ab`). **Cookie banner preview stage** — 220px-tall faux-page surface (faux nav dots + URL label) showing the live preview of the chosen banner style. Style picker: bottom-bar (default) / corner-card / centered-modal (Pro+ tier-gated via `gateAction('cookie-banner','pro')`). Save preferences button only enables on dirty state. Cross-link to creator's published Cookie Policy at `app-page-legal.html#cookie`.
- **Section 3 — Personal data on tadaify** — 5 stat cards (8 pages / 142 blocks / 1,847 newsletter subs / 63 bookings / 42.7 MB uploads). `View detailed breakdown` expander reveals 12-row table with category + count + storage location (users / pages / blocks / subscribers / doi_log / email_events / bookings / pageviews / click_events / S3 / invoices / audit_log / user_consent).
- **Section 4 — Policies you've accepted** — 5-row table with policy name + version pill + accepted-on timestamp + View link to `app-page-legal.html#<anchor>`. Subprocessor list shows `v4 available` warning chip + `View what changed` link (re-acceptance required on material changes).
- **Section 5 — DPA & subprocessors (Pro+)** — fully visible on every tier per `feedback_no_blur_premium_features`; tier-banner appears above the section when current tier < Pro, and the DPA download / subprocessor-notify toggle wire `gateAction(…, 'pro', …)` so the gate only fires at click. Three rows: Download DPA (v3, 12 pages, SCC module 3 / GDPR Art. 28), Subprocessor change notifications toggle (30-day advance notice contract), Current 4 active subprocessors (Stripe / Resend / Cloudflare / AWS).
- **Section 6 — Right to be forgotten (Art. 17) cross-link** — `erasure-card` with red-tinted icon + clear copy explaining the deletion flow lives on the Danger zone tab; `Go to Danger zone tab` link points at `app-settings.html#danger`. No deletion controls duplicated here.
- **Modals (centered 720px, NEVER drawers per `feedback_no_right_side_drawers`)**:
  - **Request export modal** — 8 includable categories as `export-include-row` cards (checkbox + name + meta), all default checked. Format pill explains ZIP+JSON+CSV+README structure. `export-progress` panel with spinner + animated bar reads "Preparing your data… (~2 min for a typical creator)" with reassuring "you can close this dialog — we'll email you when it's ready" copy. Generate-export button transitions through queued → pending → auto-closes → ready notification surfaces at top of page.
- **Demo toolbar** — tier switcher (Free / Creator ✓ / Pro / Business) controls DPA tier-banner visibility and gate behaviour; export-state switcher (Default / Pending / Ready) toggles the top notification + Request-export button label between idle / spinner / "Request another export".
- **Cross-links** — left rail to other settings tabs (Account / Billing / Security / API / Team / Danger zone) all anchored to `app-settings.html#<tab>`. Cookie Policy + Privacy + ToS + Subprocessors + AUP all link to `app-page-legal.html#<anchor>`.
- **Sidebar** uses canonical `data-partial="app-sidebar" data-active="settings"` per `feedback_sidebar_pages_grouping`.
### 2026-04-26 — Billing settings tab deep mockup (F-APP-SETTINGS-BILLING-001 #34)

- **NEW** `app-settings-billing.html` — stand-alone deep dive of `/app/settings#billing` per DEC-073 hybrid (read-only plan + invoices + Stripe Portal CTA).
- 10-state demo toolbar lets reviewers switch subscription state live without typing — covers every ECN-BILLING-NN edge case from issue #34:
  - **creator-active** (default) — full-tier render with locked-for-life pill, 12 invoices, custom domains
  - **free** — reduced layout (no invoices, no portal, no domains; upgrade CTA front-and-centre per BR-BILLING-005)
  - **creator-trial** — info banner with 5-days-left countdown + cancel-anytime cross-link
  - **past-due** (ECN-BILLING-01) — danger banner + Update-card CTA + Past-due chip + failed-renewal row in domains
  - **cancel-scheduled** (ECN-BILLING-08) — orange "Active until Dec 1, 2026" chip + Reactivate CTA + AP-029 grace-period note + cross-link hidden (redundant)
  - **card-expiring** (ECN-BILLING-02) — yellow preemptive note + warn chip in payment-method card
  - **zero-invoices** (ECN-BILLING-03) — empty invoices state with "first charge on May 1, 2026" copy
  - **eur-currency** (ECN-BILLING-06) — EUR-prefixed amounts (€8.00 EUR) in plan card + invoice rows
  - **multi-sub-items** (ECN-BILLING-07) — Creator + Workshop add-on shown as separate line items in plan card
  - **business-yearly** — yearly cadence ($490/yr · save 2 mo) + locked-at-monthly chip
- **Locked DECs honoured:**
  - DEC-PRICELOCK-01 — amber "🔒 Locked for life at $X/mo" pill on plan card + linked guarantee note
  - DEC-PRICELOCK-02 — universal $2/mo custom-domain add-on rendered as line items
  - DEC-073 — plan + invoices read-only; portal CTAs for transactional changes; cancel routed to Danger zone (NOT portal)
  - AP-010 — canonical cancel = Danger-zone modal (3-step reason → confirm → done) reachable via cross-link from Billing tab
  - AP-029 — "your pages stay live until [date]" grace-period messaging on cancel-scheduled banner + cancel-confirm modal
  - AP-031 — no sticky upsell banners; only the upgrade CTA card
- **Compare-plans inline expander** mirrors `pricing.html` in 4-card grid with current-plan highlight; uses **REAL prices** ($0 / $8 / $19 / $49 — not example numbers).
- **Monthly/yearly cadence toggle** drives both compare grid and plan card price (yearly = 10/12 of monthly per task brief; "save 2 months" callout).
- **Stripe Portal redirect modal** — spinner → success state for card update flow; real impl will `POST /api/billing/portal-session { flow }` then `window.location.href = res.url`.
- **Change-plan modal** with prorate calculation ($7.33 for 11 days remaining) + locked-for-life reassurance.
- **Cancel-flow centered modal** (3-step: reason radios with 6 options + free-text textarea → confirm with what-happens-after summary → done with reactivate option) — NEVER a drawer per `feedback_no_right_side_drawers`.
- Tab nav cross-links to sibling `app-settings-<tab>.html` files (Account / Security / GDPR / API / Team / Danger) on parallel worktrees per task brief.
- Sidebar = canonical shared partial (`data-active="settings"`); tier-switcher in demo toolbar re-renders sidebar live.
- Help footer with billing-support link + GDPR-compliant + 30-day-money-back trust strip.

### 2026-04-26 — Security settings tab deep mockup (F-APP-SETTINGS-SECURITY-001 #35)

`app-settings-security.html` (NEW). Deep-dive page for the Security sub-tab of `/app/settings`. Sister to the other three tab deep-dives currently in flight (#33 Account, #34 Billing, #36 GDPR). Sub-tab nav at top with Account / Billing / Security (active) / GDPR & data + cross-link "All settings" back to `app-settings.html`. Two-column shell on desktop ≥1180 px (main column + 320 px right-side recommendations rail), single-column on smaller screens.

- **Password section.** "Last changed N days ago" pill with 90-day reminder, primary `Change password` CTA opens a centered modal (NEVER a drawer per `feedback_no_right_side_drawers`) with current/new/confirm inputs (each with a show/hide eye toggle), a 4-bar zxcvbn-style strength meter (Weak / Fair / Good / Strong colour-coded), a live requirement checklist (12 chars + mix letters/numbers + lowercase+uppercase + not-common-pattern — each row toggles `is-met` as the user types), inline error states for AC-2 (min 12 chars), AC-3 (passwords don't match), AC-4 (current incorrect — type literal `wrong` to demo). Save button stays disabled until all three constraints pass. `Sign out everywhere` companion CTA covers the password-less variant of the same operation.
- **Two-factor authentication section.** Status pill (Enabled green / Disabled red) keyed off `body[data-state]`. 3-step centered-modal setup flow with a stepper at the top: (1) decorative QR-style SVG + base32 secret + Copy + ±30 s clock-skew note (ECN-06); (2) 6-digit input with auto-numeric-strip + length-gated Verify button + AC-6 demo (entering literal `000000` rejects with `Invalid code — try again`); (3) 10 backup codes shown once with Copy-all + Download-as-`.txt` (real Blob download) + Print + an acknowledgement checkbox that gates the final `Done` button. ECN-01 protective prompt: closing the modal at step 3 (backdrop click, ×, Esc) opens a second confirm modal "Have you saved your backup codes?" Disable-2FA confirm requires password + recovery email and surfaces ECN-09 cleanup note (deletes unused backup codes). Recovery section explains ECN-02 (no auto-recovery, `mailto:support@tadaify.com?subject=TOTP%20account%20recovery` link).
- **Active sessions section.** 5 sample rows covering ECN-03 (two simultaneous Lisbon devices on the same Wi-Fi with `Same Wi-Fi` chip), ECN-05 (a Berlin Windows row marked stale "27 days — will auto-expire in 63 days"), and a "This session" pill on the current row whose Sign-out button is disabled. Per-row Sign-out triggers a `confirm()` then fades + removes the row. `Sign out all other sessions` opens a confirm modal that lists every soon-to-be-revoked device.
- **Login history section.** 12 sample rows covering all event types — `login_success` (✅), `login_failure` (⚠️ + light red row tint), `password_changed` (🔑), `totp_enabled` (🔐), `sign_out` (🚪) — plus method pills (Password / Password+2FA / Magic link / Google OAuth) colour-coded per provider. Filter tabs (All / Successful / Failed) and a free-text search box that greps across IP, device, location. CSV export footer button. ECN-10 footnote on the bottom-of-section status strip ("Older entries pruned automatically").
- **Connected accounts section.** OAuth provider grid (Google connected as `alexandra@gmail.com` + GitHub/Apple disconnect-able) with brand-coloured icon tiles (Google = white, GitHub/Apple = dark). Disconnect opens the dynamic provider-name confirm modal. Inline help: "Disconnecting doesn't sign you out".
- **Recommendations rail.** Live security score "82/100 · Good" pill (green when 2FA on, warm when off — the visual hooks make the no-2FA state feel actionable). 4 prioritised recommendations (high/med/low priority dots): Enable-2FA (only renders when `data-state="no-2fa"`, opens the TOTP setup modal), Update-password (always present), Review-sessions (always present, smooth-scrolls to the section), Connect-backup-sign-in (marked done because Google is connected). About-this-tab card explains "security is free for every tier" + names the underlying primitives (Supabase Auth MFA RFC-6238 + bcrypt cost-12 backup codes + 30/90-day prune windows).
- **Sticky save bar.** N/A for security (every change applies via its own modal). Replaced with an inline `All changes saved automatically` green status strip in the page header — explains why there's no Save button.
- **Demo toolbar.** Tier switcher (Free / Creator / Pro / Business — security identical across all tiers per industry consensus, never paywalled, mirrors `feedback_no_blur_premium_features` even though no actual gates exist) + State switcher (filled / no-2FA / suspicious-activity-detected). The `no-2FA` state surfaces a warm banner above the section grid pushing 2FA enrol; the `suspicious` state surfaces a danger banner with 3 failed sign-ins from Berlin + auto-jump CTA into Login history (the failed rows tint a deeper red). Quick-open buttons jump straight into Change-password / 2FA-setup / Backup-codes view modals.
- **Acceptance + edge case mapping.** AC-1..AC-12 from issue body all exercised in the mockup (password happy path / 12-char / mismatch / wrong-current / TOTP correct / TOTP wrong / one-time backup codes + ack / regenerate confirm / sessions list with This-session badge / per-row sign-out / sign-out-all-others confirm / login history with ✅⚠ icons + 30-day cutoff). ECN-01..ECN-10 referenced inline as comments in the mockup HTML head + woven into the visible UI: ECN-01 (close-warn-modal), ECN-02 (recovery copy + mailto), ECN-03 (same-Wi-Fi duo), ECN-04 (rate-limit lockout banner copy in suspicious state), ECN-05 (90-day stale row), ECN-06 (clock-skew note), ECN-07 (modal-info heads-up on 2FA stays on), ECN-08 (regenerate-immediately copy), ECN-09 (disable cleanup note), ECN-10 (auto-prune footnote).
- **Cross-link contract.** Tab nav links to `app-settings-account.html` / `app-settings-billing.html` / `app-settings-gdpr.html` with a graceful fallback (`onclick` redirect to `app-settings.html#<pane>`) for cases where a sister deep-dive hasn't shipped yet — sister PRs override the fallback once their files land.

### 2026-04-26 — Custom page template (F-PAGE-CUSTOM-001 #14)

`app-page-custom.html` (NEW) + `creator-custom-public.html` (NEW). The most flexible page template — a creator-named blank page built directly from the block library. Per the issue (DEC-MULTIPAGE-01 + page-type framework), Custom is the **default** page type — most pages (homepage, ad-hoc landings, workshop pages, press kits, FAQ-style pages, anything that doesn't fit the prescribed Blog/About/Portfolio/Newsletter/FAQ/Schedule structures) are `type='custom'`. No layout shell, no type-specific config schema (per-type config = `{}`), no required blocks — just a stack of any of the 12 BLOCK_TYPES rendered with global theme/animations.

- **`app-page-custom.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > <Page name>` (live-updates from inline title input). Page header carries the **inline-editable page title** (dashed-border on hover, brand-primary focus ring), URL pill (live-updating from slug input), `Use template` button (opens 6-card starter modal), `Preview` button. Three section panels:
  - **Page settings** — URL slug input with `tadaify.com/alexandra/` prefix + **reserved-slug collision warning** (live-checks against `blog`, `about`, `portfolio`, `subscribe`, `contact`, `faq`, `book`, `admin` — those are built-in page templates), Publish toggle, Show-in-nav toggle, Hide-from-search-engines (`noindex`) toggle, 8-swatch theme background picker (matches Blog/About FIX-DIV-001 set), expandable SEO panel (meta title + description with ✨ Suggest, auto-generated OG image toggle).
  - **Page options** — power features fully visible per `feedback_no_blur_premium_features`, all carrying inline `tier-badge` + `tier-hint`, gated only at Save: per-page **custom domain** override (Pro — e.g. `presskit.alexandra.com` points just at this page), **password protection** with toggle + password input + 🔑 prefix (Pro — visitors get a Locked screen until they enter the password), **redirect after viewing** with seconds + URL inputs (Pro — sends visitors to another URL after N seconds; rare-use, leave 0 to keep page sticky). Each premium control wires `markPremiumDirty(key)` so only actually-changed options trigger gates at save (mirrors blog/about/newsletter pattern).
  - **Page content** — THE primary editor surface. Vertical drag-reorderable block list with HTML5 drag-and-drop (`is-dragging` / `is-drag-over` visual feedback, before/after insertion based on cursor Y vs row midpoint). 8 sample blocks demonstrate 6 distinct types (Heading × 3, Image, Link × 5, Accordion, Newsletter, Warm CTA Link). Each row shows: drag grip + block-type icon + block name + UPPER-case type pill + 1-line summary (up to 460px ellipsis) + flag chips (A/B active counter "312 / 287" on Block 4, Scheduled date range "May 1 – Aug 31" on Block 6) + Edit button (jumps to `app-block-editor.html?type=<type>&id=<id>`) + kebab menu (Edit / Duplicate / Schedule visibility / A/B test variants / Hide from public / Delete with confirm). Inline `+ Add block here` dashed-border CTA between block 6 and 7 + bottom `+ Add another block` full-width CTA + sticky `Add block` button in the section-head — three entry points to the picker.
- **Modals (centered, NEVER drawers per `feedback_no_right_side_drawers`)**:
  - **Add-block picker modal** (960px) — picker-search + picker-grid of all 12 BLOCK_TYPES (Link · Social · Embedded · Video · Image · Heading · Divider · Newsletter · Countdown · Product · Accordion · Custom HTML). Each card has icon + name + 1-line description. Custom HTML carries inline Business tier badge + adding it triggers `TierGate.checkAndProceed` immediately so the creator sees the gate before the editor opens. The full picker (with categories, AI suggest, Most-clicked badges) lives in `app-block-picker.html` — the modal in this editor is a quick-pick + a clear cross-link to the full one.
  - **Starter templates modal** (960px) — 6 ready-made compositions: **Press kit** (Heading + Image + Heading + Link × 4 + Heading + Accordion + Newsletter + Link), **Speaking** (Heading + Embedded + Heading + Image × 4 + Accordion + Link), **Resources** (Heading + Link × 6 + Divider + Newsletter), **FAQ-style** (Heading + Accordion × 8 + Link — with a parenthetical pointing creators to the dedicated FAQ template at `app-page-faq.html` for the full search + TOC experience), **Coming soon** (Heading + Image + Countdown + Newsletter), **Thank you page** (Heading + Image + Heading + Link × 3 — confirmation page after purchase / signup with next-step CTAs). Each card shows a `tc-blocks` chip strip listing the block types the template installs so creators can see at-a-glance what they'll get. Picking a template **replaces** the current block list (page settings stay as-is); production wires a confirm modal + undo toast.
- **Empty state** — when `?state=empty` (or block list is genuinely empty), the editor swaps to a friendly empty-state card with `🧩` emoji + "Start with a blank canvas — or pick a starter" headline + dual CTAs (`Add your first block` primary opens picker · `Browse starter templates` ghost opens template modal) + the same 6 starter-template cards inline as a 3-column responsive grid (so creators see the templates without an extra click).
- **Tier gating per `feedback_no_blur_premium_features`** — every premium element is fully visible and interactive on every tier. Custom domain field, password toggle + input, redirect-after seconds + URL, Custom HTML block in picker — all rendered with inline `tdf-tier-badge` (canonical pill, color-coded — Creator teal / Pro indigo / Business amber) plus a `tier-hint` strip explaining the gate. `repaintTierBadges()` re-renders the badges with `is-included` muted state when the demo toolbar swaps to a tier that already covers the feature (so creators on Pro see "✨ Pro" muted next to custom-domain, while Free creators see it bright). The `trySave()` handler collects only `premiumDirty` keys and runs `TierGate.checkAndProceed` from the shared `partials.js` (the canonical TierGate modal shipped in PR #76). Modal lists the gated changes and offers Upgrade → / Save without premium changes / Cancel — exactly matching the pattern locked in PR #75 / #79 / #80 / #81 / #82.
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. SEO panel is a `<details>` expander — that's *organisation*, not advanced-mode gating. Power fields (custom domain, password, redirect, noindex, theme override, all per-block kebab actions) are inline + visible-by-default-empty.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the editor JS post-injects a "Press kit" sub-item above the disabled "+ Add page" so the active Custom page is highlighted under Pages — same post-inject pattern as `app-page-blog.html` / `app-page-about.html` / `app-page-faq.html`. (Production: when DEC-MULTIPAGE-01 ships multi-page, the sidebar renders all of the creator's pages as sub-items natively; the post-inject step goes away.)
- **Sticky save bar** at bottom (left-padded under the sidebar at ≥720px / ≥1180px) with auto-save status + Discard / Save changes; `showAutosave(text)` flashes a transient status after every drag-reorder / duplicate / delete / template apply.
- **Demo toolbar (`app-page-custom.html`)** — bottom-right pill (positioned above the sticky save bar) swaps State (filled / empty) + Tier (Free / Creator / Pro / Business) live without reload, so reviewers can exercise the empty state, the reserved-slug warning, the tier-gate save flow (set custom-domain on Free → save → see modal), the Custom-HTML-via-picker gate (Free + click Custom HTML → see modal), and the template apply flow end-to-end.

- **`creator-custom-public.html`** — visitor view at `tadaify.com/<handle>/<slug>` (slug is creator-defined per the issue's "no special layout shell" rule — Custom = no shell by definition). Three sub-views demoed in one file via in-page hash routing (mirrors `creator-blog-public.html` / `creator-portfolio-public.html` / `creator-faq-public.html` patterns):
  - `#/` — full page render. Themed creator nav at top (avatar + handle + 5 page links with current page highlighted), page hero with title + sub-text from page settings, then a max-700px block stack rendering all 8 blocks the creator added: Image (founder portrait with placeholder gradient + caption), Heading × 2, Link buttons × 4 (logo downloads with secondary metadata "SVG · PNG · 2 MB"), Accordion (5 native `<details>` Q&A — keyboard + screen-reader friendly out of the box), Divider, Newsletter signup (form with email input + Subscribe button + GDPR trust line), warm-styled Contact CTA Link button.
  - `#/password` — Pro+ password gate. Centered lock screen with `🔒` emoji + headline + sub-text + password input (centered, letterspaced) + Unlock button + inline error state on wrong password + "Forgot? Contact Alexandra →" link routing to Contact page. Mockup hint shows working password `atelier-2026` matching the editor's default; entering it routes to `#/`.
  - `#/blocks-empty` — friendly placeholder for a published-but-empty page (creator hit Publish prematurely). `🚧` emoji + "This page is being built" headline + 4 link chips back to other pages (Home / Blog / Portfolio / Contact). Creator nav still renders so visitors can navigate elsewhere.
- **Theme inheritance** via `tokens.css` (Indigo Serif palette + Crimson Pro display + Inter body + same radial-gradient body wash as `creator-public.html` / `creator-blog-public.html`) so the Custom page reads as a sub-page of the creator's main site regardless of the slug.
- **Mobile-responsive** — creator nav links wrap, block stack stays max-700px and tightens gap from 18px → 14px below 600px, link buttons compress padding, lock screen stays centered.
- **Mockup-only top strip** (sticky, backdrop-blur) — Back link + URL pill + sub-view picker pill (Page / 🔒 Password gate / Empty state) + dark-mode toggle. Disappears in production. Lets reviewers click each route without typing hashes.
- **Per-issue scope honored** — implements every in-scope item from #14: registers `type='custom'` (no per-type config schema), editor renders standard block list with no extra type-specific config panel, renderer renders blocks in stack with no layout shell wrapper, designed-as-default for new pages. Out-of-scope items from #14 (special layout shell, type-specific analytics module, type-specific SEO beyond standard `pages.title` + `pages.description`) are correctly absent — the page-options section adds standard cross-page options (custom domain / password / redirect / noindex) not Custom-specific ones.

Files: `mockups/tadaify-mvp/app-page-custom.html` (NEW), `mockups/tadaify-mvp/creator-custom-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added under Page templates), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).
### 2026-04-26 — Links archive page template (F-PAGE-LINKS-ARCHIVE-001 #23)

`app-page-links-archive.html` (NEW) + `creator-links-archive-public.html` (NEW). Implementation of the page-template framework for the Links archive page type — a dedicated browse-all-links sub-page for creators with 50+ links who want a "browse the full catalogue" surface beyond the curated homepage. **Unique architectural pattern**: unlike Blog (creator writes posts) or Portfolio (creator adds projects), Links archive is **derived** from existing link buttons across the creator's site. The editor flips on which source pages contribute, and the visitor sees a searchable + filterable + grouped list. Pairs the creator-side editor with the visitor-side public render so refinement/implementation has a complete contract for both surfaces.

- **`app-page-links-archive.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > Links archive`. Page header carries the URL pill (`tadaify.com/alexandra/links`) + Preview button + 4 alt title/slug quick-pick chips (All links / Browse all / Links archive / The full catalogue) + ✨ Suggest more (cycles a small pool: Catalogue / My world / Everything / Index / Library / The vault / Browse all / Reference shelf — derives slug live).
- **Five section panels:**
  - **Page settings** — title (default "All links"), slug (default `/links`), Publish toggle (on), Show-in-homepage-footer toggle ("Browse all links →" pill below blocks; on), 8-swatch theme color picker, expandable SEO panel (meta title + description with ✨ Suggest + OG image with Generate button).
  - **Where do the links come from?** (the load-bearing unique part) — 5 source-page toggle rows in a checklist:
    - **Homepage main page** (default ON, 87 links, marked "default") — every Link button block on `/alexandra`.
    - **Blog post bodies** (default ON, 42 links) — pulls every link button placed inside a blog post body; appears under "From blog" group.
    - **About page sections** (default ON, 9 links) — press / contact / story-timeline links from `/alexandra/about`.
    - **All custom pages** (default OFF, 0 today) — future pages from the page-template framework.
    - **Manually-added bonus links** (default ON, 5 links) — archive-only links that don't appear on any other page; edited inline in the section below.
    Each row has a `src-ico` (color-coded by source type), name + sub-line, count badge, and a toggle that recolors the row when active. Underneath: a **live-count summary card** (`live-count` panel with indigo→warm gradient) showing "143 links from 4 sources · Counts refresh hourly · Recount now →" (Recount-now action ellipsis-then-restores the number to demo the flow). Recomputes live as toggles flip.
  - **Manual bonus links** — reuses the `BLOCK_TYPES.linkButton` primitive from `app-block-editor.html`. Drag-handle list of 5 sample rows (with mocked icons + titles + URLs + one A/B-test chip). Each row collapsed shows handle + 36px gradient icon + title + URL + Edit + Delete; clicking Edit toggles the row to `is-editing` state, expanding an inline form with: title input + icon emoji/upload + URL + **A/B test variant dropdown** (Off / 50/50 auto-pick / Manual split) carrying a `chip warm` Business badge + `tier-hint` + **Schedule visibility datetime** carrying a `chip` Creator+ badge. Save and Cancel buttons close the editor. + Add manual link button creates a new row, bumps the count, and recomputes total. Unlimited on every tier (cap is at the user's patience, not at the plan).
  - **How visitors browse** — two field rows:
    - **Grouping mode** — visual radio with 5 cards (`group-card`) each carrying a mini SVG-style div thumbnail rendering the layout idea: Auto by source page (default, marked `chip` "default") · Manual categories (Creator+ teal chip — creator defines own groups like "Music"/"Merch"/"Podcasts") · Alphabetical · Most clicked (Creator+ — pulls from last 30 days analytics) · Newest first. Selecting a Creator+ mode marks `premiumDirty` so Save handler picks it up.
    - **Visitor controls** — 4 toggle rows in `visitor-controls` stack. (1) Search bar (free, default ON). (2) Tag filter chips (Creator+ teal chip, default ON, with `tier-hint` strip). (3) Sort dropdown for visitors (Pro+ indigo chip + `tier-hint t-pro`). (4) Show click counts on every link (Pro+ indigo chip + `tier-hint t-pro`).
  - **Excluded from archive** — 5 sample rows (excluded-row) showing why specific links are hidden from the archive even though they live on other pages: Old support form / Internal team Notion / Affiliate code expired / "Coming soon" placeholder / Draft podcast episode. Each row shows title + URL + reason field + Edit-reason / Restore buttons. Reason editor is a **centered modal** (NEVER drawer per `feedback_no_right_side_drawers`) — 560px wide, displays the URL (read-only) + reason textarea, Save/Cancel footer. Restore animates fade-out and decrements the count.
- **Empty state** — when `?state=empty`, the editor swaps to a friendly empty-state card with "No links to archive yet" headline + 🗂️ emoji + 2 CTAs ("Add your first link button on the homepage" → links to dashboard, "Or add a manual archive-only link" → adds row inline).
- **Sticky save bar** at bottom of page with auto-save status + Discard / Save changes; save handler builds `features[]` from `premiumDirty` Set and calls `window.TierGate.checkAndProceed` from the shared `partials.js` (canonical TierGate modal shipped in PR #76). Demo toolbar lets reviewers swap state + tier live to exercise the gating flow.
- **Tier gating per `feedback_no_blur_premium_features`** — every premium element (manual-categories grouping, most-clicked grouping, tag filters, sort dropdown, public click counts, A/B test on manual links, schedule on manual links) is fully visible + interactive on every tier; an inline `chip` badge sits next to each premium label and a `tier-hint` strip explains the gate; the actual gate happens at Save via `TierGate.checkAndProceed`. Demo toolbar lets reviewers swap tier live to exercise the included-vs-required visual states.
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. Power fields (manual link inline editor, A/B test config, schedule visibility, source toggles, grouping modes, visitor sort) are inline + visible-by-default-empty, not hidden behind a mode switcher. SEO panel is a `<details>` expander — that's organisation, not advanced-mode gating.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the editor JS post-injects a "Links archive" sub-item above the disabled "+ Add page" so the active page is highlighted under Pages — same post-inject pattern as `app-page-blog.html` / `app-page-portfolio.html` / `app-page-about.html` / `app-page-newsletter-signup.html` / `app-page-faq.html`. (Production: when DEC-MULTIPAGE-01 ships multi-page, the sidebar renders all of the creator's pages as sub-items natively; the post-inject step goes away.)
- **`creator-links-archive-public.html`** — visitor view at `tadaify.com/<handle>/links`. Mockup-only top strip with back-link + URL pill + theme toggle. Creator identity row (avatar + name + handle + subtitle line; "Back to main page" pill on the right). **Page hero** with title + clamp-sized count badge ("143 links" indigo pill) + lede explaining the archive's purpose. **Sticky search + sort bar** with `backdrop-filter: blur(12px)` and indigo focus rings — search input has clear-× button that appears once you type; sort dropdown carries 4 options (Group by source page default / A → Z / Most clicked / Newest first) — re-orders rows visually within each group via a stable comparator on click. **Tag chip bar** below renders 9 sample tags (Music · Podcasts · Press · Merch · Video · Writing · Behind the scenes · Archive) + an "All" reset chip — each chip carries a `chip-count` count pill; clicking routes to `#/tag/<slug>` (Creator+ feature; rendered fully in this mockup so reviewers see the filtered state). **Results meta strip** appears below the chips for `#/group` / `#/tag` / `#/search` views, surfacing context + "Show all" / "Clear filter" / "Clear search" links.
- **Four sub-views via in-page hash routing** (mockup-only sub-view picker pill at bottom-left lets reviewers click each route without typing hashes; disappears in production):
  - `#/` — full archive default. 4 grouped sections rendered: From homepage (87 · 6 sample rows including Spotify / Tour merch / YouTube / Apple Podcasts / Bandcamp / Instagram with click counts ranging 412–3.4k) · From blog posts (42 · 4 sample rows: Tools shelf / SoundCloud demo / Pitchfork review / Song Exploder) · From About page (9 · 2 sample rows: NPR Tiny Desk / Wired interview) · Bonus archive-only (5 · the same 5 manual links from the editor).
  - `#/group/<slug>` — single group deep-link. Other groups hidden, smooth-scroll into view, results-meta strip with "Show all groups →" link.
  - `#/tag/<slug>` — tag filter. Filters across all groups via `data-tags` attribute, hides empty groups, results-meta strip with `<tag>` + count + "Clear filter →" link. Tag chip in the bar marks active.
  - `#/search?q=<query>` — live client-side search. Case-insensitive substring on title + URL; matched terms highlighted with `<span class="hi-match">` (indigo bold pill). Hides empty groups. Results-meta strip with `<query>` + count + "Clear search →" link. No-match state surfaces a friendly card ("No links matched <query>") with a Contact CTA.
- **Each link row** is a `BLOCK_TYPES.linkButton`-style card: 44px gradient icon tile (color-coded per row) · title + URL preview text + clickable tag chips · click-count badge (Pro+ visible on every row in this mockup; in production hidden when creator is on Free) · arrow icon that gradient-tints on hover. Hover lifts the card by 2px and adds a subtle shadow + indigo border tint.
- **Pagination = Load-more pattern** — single button "Load 30 more" with showing-X-of-143 meta text; hidden in tag/search views (those show their own filtered count); when full set is loaded, the button disables with "All links loaded".
- **Footer** mirrors `creator-public.html` social block — back-to-main pill, Instagram / TikTok / YouTube / Spotify / Email circular icon row, "Made with tada!ify" tag (with the locked 3-span wordmark per DEC-WORDMARK-01).
- **Theme inheritance** — visitor render uses the same `tokens.css` palette (Indigo Serif) and the same radial-gradient body wash as `creator-public.html` / `creator-blog-public.html` / `creator-portfolio-public.html` so the archive reads as a sub-page of the creator's main site. Light/dark toggle in the top strip.
- **Mobile-responsive** — link list collapses 2-col → 1-col below 720px; sticky search bar persists; tag chips wrap; sort dropdown stays accessible; sub-view picker pill stays usable. Reduced-motion preference disables the row hover transform.
- **Per-issue scope honored**: implements every in-scope item from #23 (page-type registry entry, type_config schema with search_enabled/group_by/show_dates, type-specific config panel, link_archive_list block reusing Link primitive, public renderer with search + grouping + dates, "Move to archive" pattern) and surfaces the out-of-scope items as locked deferrals (no Pocket/Pinboard/Raindrop import per DEC-APIPAGES-01 = C; no tagging beyond basic categories — yet present here for visitor experience preview; no public API / RSS feed for archive).

Files: `mockups/tadaify-mvp/app-page-links-archive.html` (NEW), `mockups/tadaify-mvp/creator-links-archive-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added under Page templates — Links archive section), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).
### 2026-04-26 — Legal page template (F-PAGE-LEGAL-001 #17)

`app-page-legal.html` (NEW) + `creator-legal-public.html` (NEW). Implementation of the page-template framework for the Legal page type. Compliance-critical surface: every creator selling on Stripe / running ads on Meta / Google / collecting EU visitor data needs at minimum a Privacy Policy + Refund Policy + Cookie Policy. Pairs the creator-side editor with the visitor-side public render so refinement/implementation has a complete contract for both surfaces.

**Relationship to admin Legal versioning** (`admin-panel.html` integrating F-LEGAL-002 #5 + F-LEGAL-003 #6): admin = founder-side versioning of the **tadaify** ToS / Privacy that every creator agrees to. This PR = the **creator's own** public Legal page, hosting THEIR policies for THEIR visitors. Distinct concept; reuses the version-tracking pattern but per-creator-policy (not org-wide). Both surfaces will eventually share an underlying `legal_documents` shape (version + effective_date + body + diff) — a future refactor.

- **`app-page-legal.html`** — canonical app-shell (top app bar + injected sidebar `data-active="pages"` with Legal sub-page highlighted post-inject + main pane + right helper sidebar on 1280+). Breadcrumb `Home > Pages > Legal`. Page header carries the URL pill (`tadaify.com/alexandra/legal`) + Preview button + 4 alt title/slug quick-pick chips (Legal / Policies / Terms & Privacy / Legal Center). Three section panels in the main column:
  - **Page settings** — title, slug, Publish toggle, Show-in-footer toggle (adds Legal link to homepage footer), 6-swatch theme color picker (suggests neutral high-contrast for readability), expandable SEO panel with **`noindex,follow` toggle defaulted ON** (recommended for legal pages — Google won't show them in results, but links to/from still work) + meta title + description.
  - **Policies** — the meat. Drag-reorderable list of policy cards. Filled state ships 5 cards: Terms of Service v1.2 (📜, expanded with full body), Privacy Policy v2.0 (🔒, expanded, GDPR variant chip, full body), Cookie Policy v1.0 (🍪, collapsed), Refund Policy v1.1 (💸, collapsed), DMCA / Copyright Policy v1.0 (©️, collapsed, Creator+ tier badge + tier-hint per `feedback_no_blur_premium_features`). Each card head shows policy name (editable inline) + version chip + effective date + last-updated + version-history icon button + kebab (Copy public link / Duplicate / Export PDF / Remove with confirm) + collapse chevron. Each card body has 3-cell meta grid (Version with `+ patch` button / Effective date `<input type="date">` / Last updated date with `today` button) + full rich-text editor matching FAQ answer pattern (Bold/Italic/H2/H3/Quote/Link/Bullet/Number list + ✨ Suggest section CTA) + auto-generated TOC entries preview (parses §1..§N from H2/H3 headings, mocked) + amber "Mockup placeholder text — replace before publishing. Have a lawyer review." warning under every body. Card foot: `View N past versions` link → opens version history modal + permalink anchor display (e.g. `#terms`).
  - **Cookie banner integration** — explainer + link to `app-settings.html#cookie-banner`. Clarifies that the cookie BANNER lives in Settings; this page is just the policy text the banner links to.
- **Compliance helper sidebar** (right column 320px on 1280+, collapses to bottom on smaller). Auto-detects what the creator needs based on their setup (mocked):
  - **Required for your setup** — checklist of 6 policies with state per row: ✓ met (Terms — selling 3 Stripe products / Privacy — 38% EU traffic / Refund — Stripe requires for paid offers / DMCA — contact form accepts attachments), ! needed (Cookie Policy — uses Google Analytics), · n/a (Accessibility Statement — optional). Auto-detect signals are mocked-realistic so reviewers grok the UX.
  - **Annual review reminder** — Last reviewed Apr 1, 2026 with stale-warning amber border + "Mark reviewed" button (bumps minor version on each policy when clicked).
  - **Translations** — Pro+ inline tier badge. List rendered for English ✓ + Polski / Deutsch / Español "Add" rows that route to TierGate. Inline tier-hint underneath.
  - **Need help?** — "Find a creator-friendly lawyer →" partner directory link (mockup-only).
- **Add-policy centered modal** (per `feedback_no_right_side_drawers`) — 9 policy-type tiles in a 3-column grid: Terms / Privacy / Cookie / Refund / DMCA marked "Added" + disabled (already on this page); 4 addable: Accessibility Statement (Pro+ inline badge), Acceptable Use Policy (Business+ inline badge), Imprint / Impressum (for German / Austrian businesses, Telemediengesetz §5), Custom policy (blank — name yourself).
- **Templates picker centered modal** — 6 vetted starter templates in 2-column grid: Generic Terms of Service (~1,200 words, UK governing law), Privacy Policy GDPR (~1,500 words, SCC for US processors), Privacy Policy US-only/CCPA (~900 words, "Do Not Sell" section), Cookie Policy (~600 words, pre-fills cookies based on integrations enabled), Refund Policy Digital (~500 words, sister templates Physical/Service/Subscription), DMCA Counter-notice (~800 words, 17 USC §512 safe-harbor compliant). Prominent amber disclaimer banner: "Templates are starting points, not legal advice." All template content is mockup-only — production templates will carry their own disclaimer + jurisdiction selection. Also reachable from empty-state CTA.
- **Version history centered modal** — small (560px). Per-policy past-versions list: current (live chip + indigo border) + N past versions with version chip + effective date range + change summary + "View archived version" icon. Tier-hint: version retention is part of Creator (Free plans keep current + previous only). Foot: "Export all as PDF" → zipped bundle of one PDF per version.
- **Empty state** — heart-of-page when no policies yet. 6 starter-template tiles + "Add a custom policy from scratch" CTA. All routes through Templates modal so creators see the disclaimer.
- **Sticky save bar** — auto-save status + Discard + Save as draft + Save & publish (`trySave('save-draft' | 'publish')`). Save handler calls `TierGate.checkAndProceed` collecting DMCA (Creator) + version retention (Creator) + multi-language (Pro). Mockup pre-wires DMCA + retention features so the gate is demoable on Free.
- **Demo toolbar** (bottom-right) — State (filled / empty) + Tier (Free / Creator / Pro / Business) live-swap. Tier badges re-render via shared `TadaifyPartials.renderTierBadge`.
- **Sidebar Legal sub-page injection** — post `partials.js` boot, a Legal sub-item is appended to the Pages accordion `is-current` (matching the FAQ pattern in `app-page-faq.html`). Demonstrates that the multi-page MVP shows the user where they are inside Pages.

- **`creator-legal-public.html`** — visitor view at `tadaify.com/<handle>/legal`. Compliance-readable design rules:
  - **High-contrast neutral background** — overrides any creator gradient. Body font Crimson Pro display at 17px / line-height 1.75 for legal-text readability. H3 sub-headings 22px display.
  - **Top strip** (mockup-only) — back link to `creator-public.html` + URL pill + theme toggle.
  - **Creator identity row** — avatar + name + handle + "Main page" link.
  - **Page hero** — H1 "Legal" + lede + meta line ("Last reviewed Apr 1, 2026 · 5 policies · 🇬🇧 English available").
  - **Policy chooser pill bar** — top-of-page navigation (📜 Terms / 🔒 Privacy / 🍪 Cookie / 💸 Refund / ©️ DMCA), each pill is `<a href="#<slug>">`, scroll-spy highlights the current section's pill as active (indigo background).
  - **Toolbar row** — "Print this page" (calls `enterPrintPreview()` → routes to `#/print`, then offers `window.print()`) + "Copy page link" (canonical URL for sharing on Meta Ads / Google Ads policy URL fields).
  - **Two-column main grid** — TOC sidebar (240px sticky on 960+, collapsible accordion on mobile) + content (max-width 760px for readability). TOC has 5 section headers + per-policy sub-anchors (e.g. `#privacy-rights`, `#dmca-agent`) for deep-link sharing.
  - **5 policy articles** rendered with full body content matching the editor's filled state (Terms 7 sections / Privacy 6 sections / Cookie 2 sections + per-cookie list / Refund 3 sections including 14-day money-back / DMCA 3 sections including designated agent block). Per-policy header carries the version chip + effective-date badge + Permalink-copy icon (real `navigator.clipboard.writeText` with toast fallback) + "Read in single-policy mode" icon.
  - **Per-policy footer** — permalink display + collapsible "View N past versions" with archived-version links that route to `#/version/<slug>/<version>`.
  - **Sub-views via in-page hash routing** so reviewers see every path:
    - `#/` — full Legal page (default)
    - `#/policy/<slug>` — single-policy focused reading mode (other articles `display:none` via `body.is-single-policy`, current article gets `.is-current-policy`). Useful for sharing a single policy without distractions.
    - `#/version/<slug>/<version>` — archived version view. Adds `body.is-archive` → persistent amber banner up top: "You're viewing an archived version. This is Terms of Service v1.1 — effective Jan 15, 2026 to Mar 31, 2026. View current version →". Also single-policy-mode (only that archived policy renders).
    - `#/print` — print-optimized layout via `body.is-print-preview`. Strips top strip + nav + TOC + chooser + actions + footer + ver-history; max-width tightened. Also wired to `@media print` so links print as `text (url)` and page breaks insert between policies.
  - **Mockup-only sub-view picker** (bottom-left demo toolbar) — `<select>` with all routes (Full / 5 single-policy modes / 2 archived versions / Print) so reviewers click each path without typing hashes.
  - **`<meta name="robots" content="noindex,follow">`** per legal-page best practice (mirrors editor SEO default).
  - **Theme inheritance** — values from `tokens.css`. Dark mode supported.
  - **Last reviewed page footer** + **public footer** mirroring `creator-public.html` (social row + "Built with tadaify" credit).
  - **Mobile responsive** — TOC collapses to top-of-page accordion (toggled with `toggleToc()`); chooser pills wrap; policy headers stack; meta-grid drops to single column.

- **All starter-template content is mockup-only PLACEHOLDER text** — clearly marked "Mockup placeholder text — replace before publishing. Have a lawyer review." in both editor (per-body amber warn box) and templates picker (top-of-modal disclaimer banner). This mockup does NOT ship vetted legal content; production will carry equivalent disclaimers + jurisdiction selection in the template metadata.

### 2026-04-26 — FAQ / Help Center page template (F-PAGE-FAQ-001 #62)

`app-page-faq.html` (NEW) + `creator-faq-public.html` (NEW). Implementation of the page-template framework for the FAQ / Help Center page type. **Distinct from the FAQ accordion BLOCK shipped in PR #70 round-2 polish** — the BLOCK is a small accordion you drop into ANY page (lives in `BLOCK_TYPES.accordion` inside `app-block-editor.html`); this PR is the full-PAGE template a creator chooses when they need a dedicated Help Center scoped at `/<handle>/faq`. Pairs the creator-side editor with the visitor-side public render so refinement/implementation has a complete contract for both surfaces.

- **`app-page-faq.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > FAQ / Help Center`. Page header carries the URL pill (`tadaify.com/alexandra/faq`) + Preview button + 4 alt title/slug quick-pick chips (FAQ / Help Center / Support / Common questions) + ✨ Suggest more. Three section panels:
  - **Page settings** — title, slug, Publish toggle, Show-in-nav toggle (adds an FAQ link to the homepage footer), 8-swatch theme color picker, expandable SEO panel (meta title + description with ✨ Suggest).
  - **FAQ behaviour** — Search-bar toggle (default ON), TOC-sidebar toggle (default ON), default-expand visual radio (3 options: First open / All collapsed / All expanded — visual previews matching the portfolio Layout pattern), "Was this helpful?" feedback toggle (Creator+ — fully visible per `feedback_no_blur_premium_features` with inline tier badge + tier-hint), Cross-section tags toggle (Pro+ — same pattern). Mocked search index preview pill ("47 questions across 6 sections · 18 distinct tags · indexed 2 min ago").
  - **Sections & questions** — composer-toolbar with editor-side search (filters Q rows live + auto-expands sections containing matches), Collapse all / Expand all buttons. Three sample sections rendered (Account & access · Billing & refunds · 1:1 coaching) with drag-grip + custom icon + editable section name + Q-count pill + collapse chevron + kebab (Change icon / Duplicate / Delete). Each section has an optional intro paragraph (dashed-border textarea, italic placeholder copy) above its Q&A list. Q&A rows show question + helpful% pill (color-coded: green ≥75%, amber <75%) + last-updated + tags + Edit button + kebab (Edit / Duplicate / Move to section / Copy deep-link / Delete). Inline `Add question to "<section name>"` button under each section's list. Bottom strip carries the AI-suggest CTA (`✨ AI suggest 5 common questions` — Creator+, fully visible) + secondary `+ Add section` button.
- **Modals (centered, NEVER drawers per `feedback_no_right_side_drawers`)**:
  - **Add section modal** (820px) — 9 section-type tiles (Account / Billing / Shipping / Returns / Product / Technical / Booking / Legal / Blank) with icon + name + 1-line description + a `✨ AI starter packs` divider followed by 4 starter cards (E-commerce returns / Course buyer / Coaching client / General help) each listing the sections it would create. Footer carries "No section cap. Free tier OK." messaging.
  - **New / Edit Q&A modal** (820px) — Section selector dropdown (with `+ Add new section…` option), Question input (no ✨ Suggest button — short text per FIX-SUGGEST-RESTORE-001 logic, AI gen value low), full rich-text answer editor with 7-button formatting toolbar + `✨ Suggest` answer button on the right (KEEPS ✨ Suggest because answers are long-form text per FIX-SUGGEST-RESTORE-001), Tags multiselect (Pro+ — fully visible with inline badge + tier-hint, gates at Save). Footer: Cancel / Delete / Save as draft / Save & publish — Save handler runs `TierGate.checkAndProceed` collecting tags-on-question (Pro).
  - **CSV import modal** (560px) — drop-zone + sample.csv download link + tier-hint explaining Pro-only gate. Triggered from the Sections & questions section header.
- **Empty state** — when `?state=empty`, the editor swaps to a friendly empty-state card with "Create your first section" CTA + AI bootstrap link + 4 starter template tiles (E-commerce returns / Course buyer / Coaching client / General help — same set as the Add-section modal so the language is consistent).
- **Sticky save bar** at bottom of page with auto-save status + Discard / Save changes; save handler runs `TierGate.checkAndProceed` collecting helpful-feedback (Creator) + cross-section-tags (Pro). Premium-dirty marker via `markPremiumDirty(key)` ensures only actually-changed premium toggles trigger gates (mirrors blog/about/newsletter pattern).
- **Tier gating per `feedback_no_blur_premium_features`** — every premium element (helpful feedback, cross-section tags, AI suggest, CSV import, tags-on-Q) is fully visible + interactive on every tier; an inline `.tdf-tier-badge` (canonical pill, color-coded — Creator teal / Pro indigo / Business amber) sits next to each premium label and a `.tier-hint` strip explains the gate; the actual gate happens at Save via the shared `TierGate.checkAndProceed`. Demo toolbar lets reviewers swap tier live to exercise the included-vs-required visual states.
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. SEO panel is a `<details>` expander — that's organisation, not advanced-mode gating. Power fields (TOC, search, helpful feedback, tags, intro paragraphs) are inline + visible-by-default-empty.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the editor JS post-injects an "FAQ" sub-item above the disabled "+ Add page" so the active page is highlighted under Pages — same post-inject pattern as `app-page-blog.html` / `app-page-about.html` / `app-page-newsletter-signup.html`. (Production: when DEC-MULTIPAGE-01 ships multi-page, the sidebar renders all of the creator's pages as sub-items natively; the post-inject step goes away.)
- **`creator-faq-public.html`** — visitor view at `tadaify.com/<handle>/faq`. Five sub-views in one file via in-page hash routing (mirrors `creator-blog-public.html` and `creator-portfolio-public.html` patterns):
  - `#/` — full FAQ (all 6 sections, default-expand applied: first section's first question open).
  - `#/section/<slug>` — single section deep-link (other sections hidden, all questions in section auto-expanded, smooth-scroll into view).
  - `#/q/<slug>` — single question deep-link (other sections hidden, target Q auto-expanded with indigo focus ring, smooth-scroll into view; permalink copy button on every Q copies this URL).
  - `#/search?q=<query>` — search results (filters questions live across all sections via case-insensitive substring match on question + answer body, highlights matched terms with `<span class="hi-match">`, hides empty sections, shows result-count banner; no-match state with friendly Contact CTA + Clear search).
  - `#/tag/<slug>` — tag filter (Pro+ public chip bar — fully rendered in this mockup so reviewers see the filtered state; in production the chip bar is hidden when the creator hasn't enabled cross-section tagging).
- **Sticky search bar** at top with backdrop-blur + clear button (× appears once you type). **Tag chip bar** below renders 10 sample tags + an "All tags" reset chip (clicking any chip routes to `#/tag/<slug>`). **TOC sidebar** sticky on desktop (left column 240px), collapsible accordion on mobile (<920px); scroll-spy via `IntersectionObserver` highlights the current section in the TOC as you scroll.
- **Q&A items use native `<details>`/`<summary>`** so keyboard + screen-reader behaviour is free. Caret icon rotates on open. Per-answer footer carries: tag chips (clickable → tag filter), "Was this helpful? Yes / No" buttons (Creator+ feature; mockup renders them by default — replaces the prompt with "Thanks for the feedback!" on click), permalink copy button (clipboard fallback to alert).
- **"Still have questions?" footer CTA** card with primary button → links to Contact page (mocked alert; production wires to the Contact page template, separate page).
- **Footer mirrors `creator-public.html` social block** — Instagram / TikTok / YouTube / Email circular icon row + "Made with tadaify" tag.
- **Theme inheritance** — visitor render uses the same `tokens.css` palette (Indigo Serif) and the same radial-gradient body wash as `creator-public.html` / `creator-blog-public.html` so the FAQ reads as a sub-page of the creator's main site. Light/dark toggle in the top strip.
- **Mobile-responsive** — TOC becomes top-of-page collapsible accordion; search stays sticky; section spacing tightens; tag chip bar wraps; subview picker pill stays usable. Reduced-motion preference disables `<details>` caret rotation.
- **Mockup-only sub-view picker** pill (bottom-left, parallel to the editor's bottom-right demo toolbar) lets reviewers click each route without typing hashes. Disappears in production.

Files: `mockups/tadaify-mvp/app-page-faq.html` (NEW), `mockups/tadaify-mvp/creator-faq-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added under Page templates), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).

### 2026-04-26 — Schedule / Booking page template (F-PAGE-SCHEDULE-001 #20)

`app-page-schedule.html` (NEW) + `creator-schedule-public.html` (NEW). Page-template framework gets its booking-page surface — built for coaches, consultants, freelancers, instructors, hairstylists, tutors, anyone who sells their time. Pairs the creator-side editor with a 7-sub-view visitor flow so refinement/implementation has the complete contract for both surfaces.

- **`app-page-schedule.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > Schedule`. Six sections: (1) **Page settings** — title (`Book a session`) + ✨ Suggest, slug `/book`, Publish toggle, theme color picker (6 swatches), expandable SEO panel. (2) **Calendar integration** is the load-bearing functional setup — 4-provider picker rendered as 2×2 card grid: Google Calendar / Apple Calendar / Outlook (all flagged `post-MVP` per the issue's locked DEC-APIPAGES-01 = C "standalone scheduler") + Manual / Tadaify-only (flagged `Recommended for MVP` and selected by default). When an OAuth provider is picked, an OAuth status panel appears (Connected as alex@gmail.com · ✓ Active · last sync 12 min ago · Reconnect / Disconnect) plus per-calendar Read-from / Write-to dropdowns. Buffer (0/15/30/60 min) + booking-window (7/30/60/90 days) + auto-detected timezone. 7-row working-hours grid: per-day toggle + start/end time inputs (Mon–Fri 9-17 default, Sat/Sun off). An info-note explicitly clarifies the MVP scope per the issue: *"the standalone scheduler ships first; calendar OAuth providers are designed here so you can see the long-term picture; Manual / Tadaify-only handles bookings end-to-end today"* — honors `feedback_mockup_full_feature_vision` (mockup shows full vision regardless of when each piece ships) without misleading reviewers about MVP scope. (3) **Booking types** — drag-reorder list with filter tabs (All 4 / Active 3 / Paused 1) + Free-tier cap reminder (`2 of 2 active types used · Upgrade for unlimited →`); per-type kebab (Edit / Duplicate / Copy direct link / View public / Pause / Delete); empty state with 4 starter templates (Free intro / Paid 1-on-1 coaching / Project consultation / Group workshop · Group prompts upgrade to Business). (4) **Availability rules** (Creator+) — recurring rules (no Mondays / lunch break) as toggle rows + one-off blackouts (May 1 holiday + May 12-18 Sicily vacation) + a 5-week May month-grid visualizer with busy/blocked/free legend (today=May 19 outlined). (5) **Notifications** split into For-creator / For-visitor: creator email (default ON, includes ICS), day-before reminder (Creator+), Slack/Discord/Notion/Custom-webhook chip row (Pro+ — chips toggle on/off live); visitor confirmation email (default ON) + collapsible Customize subject + body templates with `{{name}} {{date}} {{cancel_link}}` Mustache placeholders + ✨ Suggest, 24h reminder (Creator+), 1h reminder (Pro+). (6) **Payments** (Creator+) — Stripe Connect status panel (Active · payouts to alex@... · daily payouts) + currency picker + default cancellation refund policy (No refund / 24h / 48h / 7-day / Always full) + deposit toggle.
- **Type composer = centered modal**, NEVER a drawer (per `feedback_no_right_side_drawers`). 880px wide modal with: title + ✨ Suggest, optional cover image with AI-generate option, duration dropdown (15/30/45/60/90/120 min + Custom), Free/Paid pricing row that dims paid sub-fields (price + per-type cancellation override) when Free is on, buffer override + visibility (Public / Hidden direct-link), full **booking form fields builder** matching the Contact page pattern — Name + Email always-required (locked) + Topic (toggleable required) + Phone (optional) + Add-a-question dropdown with 10 field types (Short text, Long text, Number, Email, Phone, URL, Single choice, Multi choice, Date, File upload), description + post-booking thank-you, Active toggle. Foot: Cancel / Delete (edit only) / Save as draft / Save & activate.
- **Tier gating per `feedback_no_blur_premium_features`**: every premium element is fully visible and interactive on every tier — paid bookings, group bookings (Business · shown as paused row with explicit "🔒 Group bookings — Business plan" hint visible), recurring availability rules + 24h visitor reminder + day-before creator reminder (Creator+), webhook providers + 1h visitor reminder (Pro+), team round-robin (Business). Tier badges inline using the canonical `chip` styling (Creator teal / Pro indigo / Business amber). `trySave()` collects every premium delta (paid bookings) and runs `window.TierGate.checkAndProceed` from the shared `partials.js` (the canonical TierGate modal shipped in PR #76). Modal lists the gated changes and offers Upgrade → / Save without premium changes / Cancel.
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. Power fields (custom cancellation refund policy, deposit toggle, OAuth re-connect, recurring rules, webhook chips) are inline + visible-by-default-empty, not hidden behind a mode switcher. The SEO panel + per-visitor email customization are `<details>` expanders — that's *organisation*, not advanced-mode gating.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the editor JS post-injects a "Schedule" sub-item above the disabled "+ Add page" so the active page is highlighted under Pages — same pattern as `app-page-blog.html` and `app-page-portfolio.html`.

- **`creator-schedule-public.html`** — visitor view at `tadaify.com/alexandra/book`. Identity row (creator avatar + name + handle + bio) + a 3-step indicator pill bar that updates between active/done/upcoming as the visitor advances. **Seven sub-views demoed in one file via in-page hash routing**: `#/` Step 1 — pick a session (2-col responsive grid of 4 booking-type cards: cover + duration pill + price pill + description + Pick-a-time CTA). `#/type/<slug>` Step 2 — pick a time (recap chip with selected type emoji + Change link, 2-panel grid: left panel = month-grid calendar with `cal-day` cells colored by has-slots / is-blocked / is-selected / is-today, prev/next month chevrons, timezone selector, Find-next-available pill; right panel = up to 8 time slots in a 2-col grid with selected-state, "5 slots open this day · 15-min buffer between bookings" meta line, sticky Continue CTA, Back link). On mobile (<480px) the calendar collapses to a horizontal `.month-strip` with scroll-snap day cards. `#/type/<slug>/time/<iso>` Step 3 — fill details (recap chip + 2-panel grid: left = booking form with Name + Email + Topic required + optional Phone + Stripe Elements pretend block (card number + expiry/CVC split + "Secured by Stripe · No charge if you cancel more than 24h before" trust strip) + cancellation policy preview block; right = side recap panel with Session/When/Where/Timezone/Length + total price-tag + "What happens next" reassurance). `#/booked` Confirmation — 🎉 emoji moment + booking summary card (Session / When / Where / Paid + receipt #) + Download .ics + Add to Google Calendar buttons + Reschedule/Cancel links + Book-another link. `#/error` Friendly retry — 😬 + "Slot no longer available · card not charged" card + Pick-another-time primary CTA + Start-over secondary + email-creator fallback. `#/cancel/<token>` — 🛑 + booking summary + full-refund preview + Reschedule-instead alternative + danger-styled Yes-cancel button. `#/reschedule/<token>` — 🔄 + currently-booked summary + "Free reschedule — first one's free" reassurance + 3 quick-pick day cards (Wed / Thu highlighted / Fri) + Open-full-calendar fallback to Step 2 + Cancel-instead link.
- **Theme inheritance + mobile-responsive**: matches creator's theme (Indigo Serif) per `tokens.css`. Calendar widget collapses to a horizontal month-strip on narrow viewports (<480px); 3-step layouts collapse to single-column on <880px. Demo toolbar swaps sub-view via the routing hash live + has a 🌗 dark-mode toggle.
- **Per-issue scope honored**: implements every in-scope item from #20 (calendar widget, booking flow visitor-side, paid-via-Stripe + free paths, ICS / Google Calendar add, cancellation page from email link, reschedule page) and shows-but-flags the out-of-scope items as post-MVP context (calendar OAuth providers — issue's DEC-APIPAGES-01 = C "standalone scheduler") so the design contract reads cold without losing the long-term vision.

Files: `mockups/tadaify-mvp/app-page-schedule.html` (NEW), `mockups/tadaify-mvp/creator-schedule-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).

### 2026-04-26 — About page template (F-PAGE-ABOUT-001 #15)

`app-page-about.html` (NEW) + `creator-about-public.html` (NEW). Second page-template after Blog (#19). Pairs the creator-side editor with the visitor-side public render so refinement/implementation has a complete contract for both surfaces.

- **`app-page-about.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > About`. **Page settings** panel: title, URL slug with `tadaify.com/alexandra/` prefix, Publish toggle, Show-in-nav toggle, theme color picker (8 swatches per FIX-DIV-001), inline per-page **custom domain** field with `✨ Pro` badge + `tier-hint` (gated at Save), expandable SEO panel (meta title + description + OG image, all with ✨ Suggest). **Page content** is a vertical stack of section cards — Hero / Bio / Highlights / Story timeline / Press · Creator+ / Contact CTA — each card draggable (HTML5 drag-and-drop with `is-dragging` / `is-drag-over` visual feedback), collapsible (chevron expander), removable (delete with confirm). Per-section editors are real, fully-wired forms: Hero gets a photo drop-zone + display-name + tagline + social-proof line; Bio gets a 7-button rich-text toolbar + Crimson-Pro display textarea + word counter + ✨ AI-paragraph button; Highlights gets a 3-column grid (responsive to 2 then 1) of stat cards with icon-picker + value + caption + "Add highlight" tile (capped at 6); Timeline gets a list/cards style toggle + year + title + description per row + drag handle + per-row "+ Add milestone"; Press gets a 2-column grid of quote cards with attribution + add tile + Creator-tier `tier-hint`; Contact CTA gets a label + target dropdown (Contact page / mailto / external URL / booking link) + button-style pills (Pill default / Rounded square / Square / Outline) with `✨ Pro` badge + `tier-hint`.
- **Add-section flow = centered modal**, NEVER a drawer (per `feedback_no_right_side_drawers`). 820px wide modal with 6 section-type tiles (icon + name + 1-line description) + a divider + 3 ✨ AI starter templates (Musician / Author / Coach — each shows the section composition it'd add). Modal foot carries the Free-tier cap reminder ("up to 4 sections per About page") with a `See plans →` link.
- **Tier gating per `feedback_no_blur_premium_features`**: every premium element is fully visible and interactive on every tier. Custom domain input + Press section + custom Contact-CTA button styles all carry an inline `.sec-tier-badge` (canonical pill, color-coded per tier — Creator teal / Pro indigo / Business amber) plus a `.tier-hint` strip explaining the gate. The Free-tier 4-section cap is enforced at Save, not at display. `trySave()` collects every premium delta and runs `TierGate.checkAndProceed` from the shared `partials.js` (the canonical TierGate modal — see entry below). Modal lists the gated changes and offers Upgrade → / Save without premium changes / Cancel — exactly matching the pattern locked in PR #54 for `app-block-editor.html` and PR #75 for `app-page-blog.html`. Placeholder comment `<!-- TierGate placeholder -->` documents the wiring point so the implementer doesn't have to guess.
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. Power fields (SEO panel, custom domain, button-style picker, timeline-style picker) are inline + visible-by-default-empty, not hidden behind a mode switcher. The SEO panel is a `<details>` expander — it's *organisation*, not advanced-mode gating.
- **Live preview** via top-right "View public" button (and a `Preview` button next to the URL pill) that opens `creator-about-public.html` in a new tab.
- **`creator-about-public.html`** — visitor view at `tadaify.com/<handle>/about`. **Hero** with a big 280px circular photo, display name in clamp-sized display font (36–56px), Crimson-Pro tagline, social-proof inline line; collapses to single-column centered layout below 760px. **Bio section** in Crimson Pro display at 19px / line-height 1.7, max-width 720px, paragraph spacing — long-form reading typography matching the blog single-post body style. **Highlights grid** — 3 columns desktop, 2 columns tablet, 1 column mobile; cards lift on hover. **Story timeline** — alternating left/right markers connected by a vertical center gradient rail on desktop; collapses to a single-column with the marker on the left rail at <720px. **Press carousel** — 3 quote cards with dot indicators + auto-advance every 7s with hover/focus pause; horizontal scroll-snap fallback on <600px (carousel transforms disabled, dots hidden). **Contact CTA** — large pill button leading to the Contact page. **Footer** mirrors `creator-public.html` social-stack block + "made with tadaify" tag.
- **Theme inheritance**: visitor render uses the same `tokens.css` palette (Indigo Serif) and the same radial-gradient body wash as `creator-public.html` so the About page reads as a sub-page of the creator's main site.
- **Mobile-responsive**: all sections collapse cleanly to single column; timeline becomes simple vertical stack; quote carousel becomes scroll-snap horizontal scroll. Reduced-motion preference disables auto-advance carousel + fade-in animations.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the editor JS post-injects an "About" sub-item above the disabled "+ Add page" so the active page is highlighted under Pages. (Production: when DEC-MULTIPAGE-01 ships multi-page, the sidebar renders all of the creator's pages as sub-items natively; the post-inject step goes away.)
- **Demo toolbar (`app-page-about.html`)** — bottom-right pill swaps State (filled / empty) + Tier (Free / Creator / Pro / Business) live without reload, so reviewers can exercise the empty state, the tier-gate save flow, and the add-section modal end-to-end.

Files: `mockups/tadaify-mvp/app-page-about.html` (NEW), `mockups/tadaify-mvp/creator-about-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).

### 2026-04-26 — TierGate modal — shared upsell flow at save (THE single upsell scene)

`app-tier-gate-modal.html` NEW — designs the SHARED upsell modal called by every block editor + page editor + settings flow when a creator clicks Save on a feature requiring a higher subscription tier than they currently have. Sister files (`app-block-editor.html`, `app-page-blog.html`) already reference `window.TierGate.checkAndProceed(...)` as a placeholder for the post-#54 / post-#19 era; this PR ships the design only — wiring lands in a follow-up PR after the design is accepted.

**Why this modal exists.** Per the locked UX rule `feedback_no_blur_premium_features` (2026-04-26 during PR #54 review): premium features (A/B testing, custom domain, team members, scheduling beyond 7 days, Klaviyo custom fields, scheduled blocks, far-future blog scheduling) are **fully visible and fully interactive** in every editor on every tier. Free creator can configure them, fill them out, see live preview. The gate happens **only at save**. This modal IS that gate. It is the ONLY place in tadaify where "you need to upgrade" is communicated. No sticky banners (per AP-031), no popups after N days, no email nags.

- **Centered modal, never right-drawer** per `feedback_no_right_side_drawers` (640px wide, full-screen <600px).
- **Friendly framing throughout** per `feedback_tadaify_simple_ui_no_advanced_toggle`. Head: "Just one step — `<feature>` needs `<tier>`" — never "Upgrade required" / "You need to" / "Locked". Body section copy: "Your work is saved as draft" / "After upgrade it goes live in one click" / "No worries — your work is still saved as draft" / "Just one step". Notion / Linear voice end-to-end.
- **Body = 4 sections:** (1) **"You set up:"** trigger context — bulleted list of EXACTLY what creator did to trigger the gate, with `<code>` for domain/handle/values; (2) **tier feature panel** — header "`<Tier>` plan ($X/mo) gives you:" + 5-7 checkmarked features mirroring `pricing.html` per tier; the panel is colour-coded (Creator/Pro = indigo, Business = warm); the lowest-tier-that-covers-everything is selected (multi-feature triggers default to the highest required tier); (3) **"Your work is saved as draft"** reassurance — load-bearing UX promise, without it creator who walks away loses configuration; (4) **"Or save without `<feature>`"** partial-save inline link — only renders for features where partial save makes sense (A/B test = save Variant A; Schedule = publish now; Klaviyo custom mapping = sync default fields). Skipped for features where partial doesn't apply (custom domain — there's no "partial" custom domain).
- **Footer (sticky):** LEFT — `Save as draft` ghost button (closes modal, marks block as draft, no upgrade). RIGHT — monthly/yearly billing toggle (defaults to yearly per pricing.html canonical) + warm-coloured `Upgrade — $X/mo` primary button with subtext "billed yearly · save $98" when yearly is selected. Annual = 10/12 of monthly per the "Save 2 months" toggle in `pricing.html`.
- **Seven documented states**, swappable via state-strip chips at the top of the modal: **Default** (4 sections rendered, footer with both buttons enabled) · **Loading checkout** (primary button shows spinner + "Opening checkout…", everything else disabled — typically <1.5s before Stripe hosted checkout opens) · **Stripe success** (entire modal swapped with celebration: ✨ burst, "You're on Business now! Let's publish your A/B test.", Continue→Publish CTA, 12-square CSS-only confetti respecting `prefers-reduced-motion`) · **Stripe cancel** (friendly retry/exit: "No worries, your work is still saved as draft", Try upgrade again + Save as draft and exit) · **Already-on-right-tier** (TierGate.checkAndProceed silently skips — no modal, just bottom-right toast "All set ✓") · **Yearly-plan creator** (primary button reads "Upgrade — $40.83/mo" with "billed yearly · save $98" subtext, illustrating that TierGate respects existing billing cycle) · **Admin-role required** (edge case: creator already on Business but adding team members needs Admin role; modal pivots to "Your workspace already has Business — you just need Admin", primary button becomes "Notify alexandra@example.com").
- **Eight demo triggers** in the launcher cover every realistic scenario: A/B testing on Newsletter (Free → Business), Custom domain (Free → Creator), Team member invite (Pro → Business), Scheduling beyond 7 days (Free → Creator), Klaviyo custom fields (Pro → Business), Multi-feature trigger (Free → Business covering A/B + custom domain + schedule), Already-on-right-tier (no modal — toast variant), Admin-role required (workspace tier OK but role insufficient).
- **Multi-feature triggers** list every triggered feature in section 1 and propose the highest tier covering all of them (Business in the canonical example, since Business covers Pro covers Creator). One Save = one upsell, no chained modals.
- **Demo state machine in plain JS, no framework** — implementation PR will lift this to a real React/Vue/Svelte component, so think of this as the BEHAVIOURAL CONTRACT. Public API mirrors what `app-block-editor.html` and `app-page-blog.html` already call: `window.TierGate.checkAndProceed({ currentTier, features, onProceed, onSaveWithout, onUpgrade })`. `TIER_RANK` and `TIER_LABEL` are exposed on `window.TierGate` for backward compat with existing references.
- **Tier feature lists mirror `pricing.html` exactly** — Creator covers custom domain + scheduling + branding hide + 5-min refresh + 90-day window + priority support + verified badge; Pro covers everything in Creator plus real-time live view + 1-year history + saved views + Creator API + AI priority queue; Business covers everything in Pro plus A/B testing + team + identity stitching + Klaviyo custom fields + Parquet R2 + Replay. Tier prices canonical from `pricing.html`: Free $0 · Creator $8/mo · Pro $19/mo · Business $49/mo. Annual: ~$6.67 / ~$15.83 / ~$40.83 per month respectively (Save 2 months toggle).
- **Cost frame.** $0 to ship the modal itself. Real cost = paid-funnel conversion. Friendly framing > pushy framing in every SaaS A/B test we've benchmarked. "Your work is saved as draft" is the load-bearing UX promise that lets creator walk away without panicking — without it, walk-away rate spikes. This is why the modal exists, and why it's the ONLY upsell scene.
- **Scope.** This PR ships the mockup only. Wiring the `<!-- TierGate modal triggered… -->` placeholder comments left by PR #54 (in `app-block-editor.html` newsletter form, schedule visibility, A/B variants) and PR #19 (in `app-page-blog.html` post composer for premium author dropdown + far-future scheduling) into this modal lands in a follow-up PR after the design is accepted. No changes to `app-block-editor.html`, `app-page-blog.html`, `shared/partials.js`, or any other file beyond README + index + the new file.

Files: `mockups/tadaify-mvp/app-tier-gate-modal.html` (NEW), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table row), `mockups/tadaify-mvp/index.html` (link added under Phase 2.5 — Editor sub-modals).

### 2026-04-26 — Newsletter signup PAGE template (F-PAGE-NEWSLETTER-001 #22)

`app-page-newsletter-signup.html` (NEW) + `creator-newsletter-signup-public.html` (NEW). Second implementation of the page-template framework (after Blog), this time for `type='newsletter'` per the page-type registry. **Different from the inline Newsletter signup BLOCK shipped in PR #74** — the BLOCK is a small inline form on a creator's main page; this PAGE is a dedicated full-page Substack/Beehiiv-style landing whose only goal is conversion to a subscriber. Pairs the creator-side editor with the visitor-side public render so refinement/implementation has a complete contract for both surfaces.

- **`app-page-newsletter-signup.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > Newsletter signup`. Page header carries the URL pill (`tadaify.com/alexandra/subscribe`) + Preview button + 4 alt title/slug chip-templates (Subscribe / Newsletter / Stay in touch / Updates). 8 reorderable sections + a fixed Page settings row at top:
  - **Page settings (always present, NOT removable)** — title (default "Subscribe"), URL slug (default `/subscribe`), Publish toggle, theme color picker (8 swatches), expandable SEO panel (meta title + description + OG image, all with ✨ Suggest), **Custom domain** (Pro+ — fully visible per `feedback_no_blur_premium_features`, gated at Save), **A/B test variants** (Business — toggle visible at every tier, gated at Save).
  - **Hero section** — headline + ✨ Suggest with 4 inline copy templates (Get my weekly notes / Never miss a release / Join 12k creators learning X / Curated insights every Tuesday); sub-headline + ✨ Suggest; cover-image / illustration / logo with cover-drop + AI-generate fallback.
  - **Email provider section (the primary functional section)** — REUSES `BLOCK_TYPES.newsletter` from `app-block-editor.html` exactly so creators who learned the block form already know the page form. **Exactly 7 dropdown options per `feedback_tadaify_newsletter_mvp_scope_locked_2026_04_26`**: Kit (ConvertKit) · Beehiiv (DOI ON default per DEC-102) · MailerLite · Mailchimp (NO warning copy per DEC-117 user override) · Klaviyo (capture-name toggle ON per DEC-120) · Google Sheets (Sign-in-with-Google flow) · Generic webhook (versioned URL `tadaify.com/wh/v1/<creator>` per DEC-104, with copy button + payload schema details). NO Substack, NO Buttondown (deferred to v2). Each provider panel shows API key with show/hide eye + Test connection button + audience selector + source tag. All 7 panels rendered (only one visible at a time via `data-provider-panel`). Per-provider success message at the bottom. Markup carries `<!-- shared with BLOCK_TYPES.newsletter; consolidate to shared partial in implementation -->` comment so refinement knows to extract `shared/newsletter-provider.partial.{html,css,js}`.
  - **Signup form section** — 3 layout cards (One-line · Two-line · Centered card) with mini visual previews; click switches `data-layout`. Email placeholder + button label fields. **GDPR consent checkbox ON by default** with EU-compliance inline hint. Customisable consent copy.
  - **Social proof section** — style picker (count / quotes / both). Count: customisable template with `{count}` interpolation + live provider pull (mocked at 24,512). Public-count visibility carries `Creator` tier badge (Free hides above 2,500). Quote list with drag-reorder + + Add quote.
  - **What you'll get section** — drag-reorder bullets with emoji picker + 4 starter templates (Weekly notes / Monthly digest / Drop alerts / Behind-the-scenes).
  - **Past issues preview section** — Creator+ tier badge inline; toggle to show 3 most recent posts pulled from provider archive every hour. Live mocked preview (3 cards with cover + title + excerpt + read-time). Tier-hint surfaces when below Creator.
  - **FAQ section** — 3-5 Q&A pairs pre-filled with common-objection defaults (How often will you email me? / Can I unsubscribe? / Will you sell my data?). Each item drag-reorderable + removable. Add question button.
  - **Footer CTA section** — closing headline + button label + social-fallback copy + toggle to pull socials from main page.
- **Section management = centered modal section picker** per `feedback_no_right_side_drawers` (NEVER a drawer). Section picker shows 11 section types (8 default + 3 optional: Full-width image / Pull quote / Video intro). Already-added sections show an "Added" badge but can be added again. Sticky save-bar at bottom of page with Discard / Save changes; save handler carries the `TierGate.checkAndProceed` placeholder collecting custom-domain (Pro) + A/B test (Business) + past-issues (Creator) + public-count >2.5k (Creator) — exactly matches the pattern locked in PR #54 for `app-block-editor.html`.
- **Empty state** — when `?state=empty`, the editor swaps to a friendly empty-state card with "Use recommended layout" (instantly populates with 5 default sections) + "+ Add section" (opens picker). Demo-toolbar can swap State / Provider / Tier live.
- **`creator-newsletter-signup-public.html`** — visitor view at `tadaify.com/<handle>/subscribe`. Single, focused conversion page (NOT a multi-section browse). Mockup-only top strip with back-link + URL pill + theme toggle. Creator identity row (avatar + name + handle + subscriber count + frequency). Hero centered with cover icon + clamp-sized headline + lede. **Provider trust strip** ("Powered by Kit · GDPR-friendly · 1-click unsubscribe") — small, builds confidence about who's getting the visitor's email. **All 3 form layouts rendered as anchor sections** (`#layout-oneline` / `#layout-twoline` / `#layout-card`) so reviewers can compare without leaving the file — in production only the chosen layout renders; mockup carries an inline "Compare layouts" chip strip explaining this. Each layout has email field + button + GDPR consent checkbox. Submitting any form jumps to `#/submitting`.
- **All 5 confirmation states rendered as anchor sections** via in-page hash routing — same pattern as `creator-blog-public.html`:
  - `#/` — default landing (full page)
  - `#/submitting` — spinner card "Adding you to the list…", auto-advances to `#/success` after 1.5s for the demo loop
  - `#/success` — "Thanks for subscribing! Check your inbox to confirm" + add-to-contacts CTA + resend-confirmation link
  - `#/already-subscribed` — "You're already on the list. Manage on Kit →" + use-different-email link
  - `#/error` — friendly retry "Something went wrong on our end" + Email Alexandra fallback + error code (e.g. `KIT_API_502 · req_8a2b…`)
- **Mockup-only state-anchor strip** at the bottom of the public page lets reviewers click each state without typing hashes. Disappears in production.
- **Theme inheritance** — uses the same `tokens.css` palette (Indigo Serif) so the subscribe page feels like a sub-page of the creator's main site. Page-level theme override exposed in the editor would in production rebind a CSS var on the public render.
- **Mobile-responsive** — One-line form collapses to stacked email + button; past-issues grid 3-col → 1-col; hero scales via `clamp()`; trust strip wraps; consent rows stay readable.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the Newsletter signup sub-page is highlighted under Pages (post-MVP when DEC-MULTIPAGE-01 ships multi-page, the sidebar will render all of the creator's pages as sub-items natively).
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. Power fields (custom domain, A/B test, SEO panel, schedule) are inline + visible-by-default-empty, not hidden behind a mode switcher. **No blur on premium UIs** per `feedback_no_blur_premium_features` — every tier-gated section is fully readable + interactive on every tier; tier badge sits inline; gating happens only at Save via `TierGate.checkAndProceed`.

Files: `mockups/tadaify-mvp/app-page-newsletter-signup.html` (NEW), `mockups/tadaify-mvp/creator-newsletter-signup-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).

### 2026-04-26 — AI Suggest sub-modal mockup added (sister of FIX-SUGGEST-RESTORE-001)

`app-ai-suggest-modal.html` NEW — designs what happens AFTER a creator clicks the ✨ Suggest button anywhere across the block editor. PR #70 round-2 polish (FIX-SUGGEST-RESTORE-001) landed the trigger on every title / label / caption / short-text field flagged with `ai: true` in `BLOCK_TYPES.*.form()`; until now the post-click behaviour was undefined. This mockup fills the gap so the implementation PR has a clear contract.

- **Centered modal, never right-drawer** per `feedback_no_right_side_drawers` (560px wide, full-screen <600px).
- **Six states** rendered as static frames in the same file: Loaded (5 cards) / Loading (shimmer skeletons) / Error (friendly retry, no error codes) / Empty (no input — Generate from context CTA) / Rate-limited (Free 30/day cap with tier upsell — typical creator never sees per `feedback_no_blur_premium_features` + DEC-119) / Selected confirmation.
- **Context strip** at top of body shows what tadaify is feeding the AI as prompt context (block type + URL or filename or current copy). Reads "Based on your <block-type> — <salient field>".
- **Refinement textarea** visible + empty by default per `feedback_tadaify_simple_ui_no_advanced_toggle` (DEC-114). No "Show advanced" toggle. Placeholder gives 3 example tones to teach without forcing.
- **Five suggestion cards** with index marker + copy + style tag (Friendly / Direct / Playful / Professional / Curious). Click any card to select (border + background tint + check). Use this disabled until selection.
- **Sticky foot.** Refresh suggestions (left, secondary, with spinner) / Cancel (right, ghost) / Use this (right, primary, disabled until pick).
- **Standalone demo launcher** at top of file with 3 trigger buttons (Block title / Caption / Button label) — opens the live modal with the matching context strip, swappable state via demo chip strip. Lets the file be reviewed cold without embedding in app-block-editor.html.
- **Cost frame** documented in the file header (DEC-097-era cost-value). Claude Haiku ≈ $0.0001/click; 100k creators × 5 clicks/year ≈ $50/year total — cheap, on brand with marketing pillar #2 (AI baked in).
- **Scope.** This PR ships the mockup only. Wiring the trigger from `app-block-editor.html` into this modal lands in a follow-up PR after the design is accepted. No changes to `app-block-editor.html`, `shared/partials.js`, or any other file beyond README + index + the new file.

### 2026-04-26 — Portfolio page template (F-PAGE-PORTFOLIO-001 #16)

`app-page-portfolio.html` (NEW) + `creator-portfolio-public.html` (NEW). Second implementation of the page-template framework for `type='portfolio'` per the page-type registry. Pairs the creator-side editor with the visitor-side public render — three different layout modes (Masonry / Grid / Carousel) demoed in the same file so refinement can compare without context-switching.

- **`app-page-portfolio.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > Portfolio`. Three sections: (1) **Page settings** — title, URL slug with `tadaify.com/alexandra/` prefix, Publish toggle, RSS toggle (yes — portfolio entries syndicate too), theme color picker (8 swatches), expandable SEO panel (meta title + description + OG image, all with ✨ Suggest); (2) **Layout** — visual radio for 3 grid modes with mini SVG-style div previews (Masonry/Grid/Carousel), items-per-row slider (2/3/4), cover sizing dropdown (Auto/1:1/4:3/16:9/2:3/3:4), show-tag-pills toggle, visitor-filter-UI toggle; (3) **Projects** — filter tabs (All 47 / Published 42 / Drafts 3 / Archived 2), search input, sort dropdown (Newest / Oldest / Manual drag / A→Z), tag-filter chip bar (real DOM filtering on the editor table, not just the public page), project rows with drag handle + cover thumbnail (with featured-pin badge + optional video-duration pill) + title + status chip + metadata (year · medium · client) + tag pills + Edit button + kebab menu (Edit / Duplicate / Mark/Unmark featured / Archive / View public / Delete), pagination chips. Empty state with friendly illustration + "Add your first project" CTA + ✨ starter-template chips (Photographer / Designer / Architect / Illustrator).
- **New/Edit project composer = centered modal**, NEVER a drawer (per `feedback_no_right_side_drawers`). 880px wide, max-height 92vh with internal scroll, mobile-responsive. Contains: **Cover media kind toggle** (Image ↔ Video, per FIX-IMG-001 unified pattern — switching live swaps the primary preview gradient + adds a video tag), primary cover preview with crop-hint + Replace, **Gallery** (multi-image grid up to 8 images with drag-reorder + remove + Add tiles), title with ✨ Suggest, slug + year picker (2020-2026 + Earlier), **Medium / Category multi-select** dropdown (Photography / Illustration / 3D / Video / Web design / Branding / Architecture / Music / Lettering / Print / Motion / UX — extensible registry pattern like F-BLOCK-SOCIAL-001's SOCIAL_PLATFORMS), rich-text description editor (matches Blog post body — formatting toolbar + Crimson Pro display font), tags multi-select, **Project URL** (optional external case study link with `https://` prefix), **Client** field (optional, for commissioned work), **Collaborators multi-select** (Business / Team — fully visible + interactive per `feedback_no_blur_premium_features` with `🔒 Business` chip + tier-hint), **Featured toggle** (with explanatory copy + tier-hint about Free capped at 3 / Creator+ unlimited), **Schedule** datetime (Pro gate beyond 7 days). Sticky footer: Cancel / Delete draft / Save as draft / Schedule / Publish.
- **Tier gates surfaced inline + at Save** — three premium combos modeled in `trySave()`: collaborators set → Business/Team; featured toggle on while tier=free → Creator+; schedule date >7 days → Pro. The save handler builds a `features[]` array and calls `TierGate.checkAndProceed` from the shared `partials.js` helper (same modal pattern as PR #54 block-editor and PR #75 blog editor). Free-tier creators see the upgrade-prompt on Save; higher tiers proceed silently.
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. Power fields (SEO panel, Collaborators, Schedule, Project URL, Client) live inline + visible-by-default-empty, not hidden behind a mode switcher.
- **`creator-portfolio-public.html`** — visitor view at `tadaify.com/<handle>/portfolio`. Five sub-views in one file via hash routing (`#/`, `#/grid`, `#/carousel`, `#/project/<slug>`, `#/tag/<slug>`) so reviewers click through every state without leaving the file. **List view layouts** — three rendered grids gated behind a layout-switcher pill (in the page hero); switching swaps the visible pane live + updates the URL pill. (1) **Masonry** — CSS columns layout, asymmetric heights via `h-tall`/`h-mid`/`h-low` cover classes, 3→2→1 columns responsive; (2) **Grid** — uniform `aspect-ratio: 1/1` cards in a 3-column grid, identical metadata strip; (3) **Carousel** — hero stage with caption overlay + prev/next arrows + dots + 6-card thumbnail strip; clicking a thumb or dot transitions the stage with full state sync (title/year/client/tags/CTA href all update). Tag chips filter both grids simultaneously; empty-tag state surfaces when 0 match. **Single project view** — full-width cover + meta row (year / medium / client + share buttons) + Crimson Pro body (paragraphs, h2, blockquote, lists) + multi-image gallery (6 thumbnails) that opens a centered-modal **lightbox** with keyboard arrow nav + ESC + click-backdrop close + image counter, **Collaborators block** (rendered when set, mirrors the Business/Team feature), **External CTA** button (Project URL — Behance/Dribbble/etc.), tag chips that route back to filter view, related projects (3 cards). Footer mirrors `creator-public.html` social block.
- **Theme inheritance** — same `tokens.css` palette so the portfolio reads as a sub-page of the creator's main page. Body wash mirrors `creator-blog-public.html` for visual consistency across the page-template family.
- **Lightbox = centered modal** per `feedback_no_right_side_drawers` — backdrop overlay + centered stage + close button anchored top-right + keyboard nav. Never a side panel.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the editor JS post-injects a "Portfolio" sub-item above the disabled "+ Add page" so the active page is highlighted under Pages — same pattern as `app-page-blog.html`. (Production: when DEC-MULTIPAGE-01 ships multi-page, the sidebar renders all of the creator's pages as sub-items natively; the post-inject step goes away.)
- **Demo toolbar (`app-page-portfolio.html`)** — bottom-right pill swaps State (filled / empty) + Tier (Free / Creator / Pro / Business) live without reload, so reviewers can exercise the empty state, the tier-gate save flow (esp. Free creator hitting the 4-featured cap or trying collaborators), and the modal end-to-end.

Files: `mockups/tadaify-mvp/app-page-portfolio.html` (NEW), `mockups/tadaify-mvp/creator-portfolio-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).

### 2026-04-26 — Blog page template (F-PAGE-BLOG-001 #19)

`app-page-blog.html` (NEW) + `creator-blog-public.html` (NEW). First implementation of the page-template framework for `type='blog'` per the page-type registry. Pairs the creator-side editor with the visitor-side public render so refinement/implementation has a complete contract for both surfaces.

- **`app-page-blog.html`** — canonical app-shell (top app bar + injected sidebar via shared `data-partial="app-sidebar" data-active="pages"` + main pane). Breadcrumb `Home > Pages > Blog`. Three sections: (1) **Page settings** — title, URL slug with `tadaify.com/alexandra/` prefix, Publish toggle, RSS toggle, theme color picker (8 swatches per FIX-DIV-001), expandable SEO panel (meta title + description + OG image, all with ✨ Suggest); (2) **Layout** — card layout (cards/list/magazine), posts per page (1-50 per page-type schema), sort order (newest/oldest/manual-drag), show-author toggle; (3) **Posts** — filter tabs (All 23 / Published 18 / Drafts 3 / Scheduled 2), search input, post rows with drag handle + cover thumbnail + title + status chip + meta line + tag pills + Edit button + kebab menu (Edit / Duplicate / Schedule / View public / Delete), pagination chips. Empty state with friendly illustration + "Create your first post" CTA + ✨ starter-template link.
- **New/Edit post composer = centered modal**, NEVER a drawer (per `feedback_no_right_side_drawers`). 880px wide, max-height 92vh with internal scroll, mobile-responsive. Contains: cover image with crop hint + Replace, title with ✨ Suggest, auto-generated URL slug (editable until first publish), rich-text editor with formatting toolbar (Bold / Italic / Heading / Quote / Code / Link / Image / Bullet / Number + ✨ AI paragraph), tags input with chip + remove, premium **author dropdown** (Business/Team — fully visible + interactive per `feedback_no_blur_premium_features`, gates at Save via shared `TierGate.checkAndProceed`), schedule datetime picker (Pro gate beyond 7 days). Sticky footer: Cancel / Delete draft / Save as draft / Schedule / Publish.
- **No "Show advanced" toggle anywhere** per `feedback_tadaify_simple_ui_no_advanced_toggle`. Power fields (SEO panel, schedule, author) are inline + visible-by-default-empty, not hidden behind a mode switcher.
- **`creator-blog-public.html`** — visitor view at `tadaify.com/<handle>/blog`. Three sub-views in one file via hash routing (`#/`, `#/post/<slug>`, `#/tag/<slug>`) so reviewers can click through every state without leaving the file. List view: page hero (title + lede + post count + RSS + email-subscribe links), tag chip bar (All / training / recovery / mindset / nutrition / habits — clicking actually filters the grid; empty-tag state surfaces when 0 match), 2-column post-card grid (6 cards) with cover + title + 2-line excerpt + author + date + read-time + tag, pagination (desktop chips → mobile "Load more"). Single post: full-width cover, h1, meta row (author / date / read-time / share buttons), long-form body in Crimson Pro display font with h2 / blockquote / list, tag chips that route back to filter view, related posts (3 cards), comments section with provider switcher (Disqus / Hyvor / Off — content swaps live). Footer mirrors `creator-public.html` social block.
- **Theme inheritance**: creator-blog-public.html uses the same `tokens.css` palette (Indigo Serif) so the blog feels like a sub-page of the creator's main page. The same overrideable `Page background` field exposed in the editor would in production rebind a CSS var on the public render.
- **Comments provider** stays mockup-level (Disqus/Hyvor/Off) per the issue's "Scope (out): Comments system (separate story)" — the visitor view shows where it lives without committing to an integration.
- **Tier gates surfaced inline** — author dropdown carries a `🔒 Business` badge + `tier-hint` strip ("Switching author is part of Team on the Business plan…"); schedule field carries a `tier-hint` for far-future dates ("Scheduling beyond the next 7 days is a Pro perk…"). At Save the modal calls `TierGate.checkAndProceed` with the actual premium delta and offers Upgrade / Save without premium / Cancel — matches the pattern locked in PR #54 for `app-block-editor.html`.
- **Sidebar canonical** per `feedback_sidebar_pages_grouping`: shared `app-sidebar` partial renders Pages parent (accordion-expanded) with Home as the first sub-item; the editor JS post-injects a "Blog" sub-item above the disabled "+ Add page" so the active page is highlighted under Pages. (Production: when DEC-MULTIPAGE-01 ships multi-page, the sidebar renders all of the creator's pages as sub-items natively; the post-inject step goes away.)
- **Demo toolbar (`app-page-blog.html`)** — bottom-right pill swaps State (filled / empty) + Tier (Free / Creator / Pro / Business) live without reload, so reviewers can exercise the empty state, the tier-gate save flow, and the modal end-to-end.

Files: `mockups/tadaify-mvp/app-page-blog.html` (NEW), `mockups/tadaify-mvp/creator-blog-public.html` (NEW), `mockups/tadaify-mvp/index.html` (links added), `mockups/tadaify-mvp/README.md` (this entry + canonical-mockups table rows).

### 2026-04-26 — Social icons row redesign: addable cards + drag-reorder + per-platform icon variants (PR #54 extension)

`app-block-editor.html` overhauls the `Social icons row` block type per user feedback during PR #54 review. The fixed 6-platform input list is replaced with an addable, removable, draggable card list and a per-platform icon-style picker. Plus a small global change: an "A/B test" identity chip in the Content section header so the feature reads as a named capability across all 12 block types, not a footnote.

- **30 platforms supported.** `SOCIAL_PLATFORMS` registry (Instagram, TikTok, YouTube, X, Spotify, Apple Music, SoundCloud, Twitch, LinkedIn, Discord, Telegram, WhatsApp, Facebook, Pinterest, Threads, Mastodon, Bluesky, GitHub, Substack, Patreon, Buy Me a Coffee, Ko-fi, Gumroad, Snapchat, Reddit, Vimeo, Dribbble, Behance, PayPal, Etsy). Each carries `id`, `name`, brand `color`, default style. Adding a new platform = one line in the array + (optional) one extra entry in the existing `ICON_LIBRARY` brand bodies.
- **183 synthesized icon variants.** Per-platform style catalog (`SOCIAL_ICON_STYLES`): `brand` (canonical brand-coloured logo), `mono` (currentColor — adapts to light / dark), `mono-frame` (mono with a circle outline), `outline` (hollow brand outline), `filled-circle` (white glyph on brand-coloured circle), `filled-square` (white glyph on brand-coloured rounded square). Plus `SOCIAL_LEGACY_STYLES` for the 3 platforms with iconic alternates: X gets the `twitter-classic` blue bird, Instagram gets `classic-camera` (gradient camera glyph), YouTube gets `subscribe-red` (the iconic SUBSCRIBE button). 30 platforms × 6 base styles + 3 legacy = **183 total variants**, all rendered from `renderSocialVariant(variantId, size)` with no inline-SVG bloat (each style is a wrapper around the canonical brand body kept in `ICON_LIBRARY`).
- **`social-cards` field kind.** Replaces the old `social-list` (fixed 6 input rows). Default state: `state.handles = {}` (empty), `state.handlesOrder = []`. The form renders: a primary `+ Add social` button + a count chip ("N platforms added") + a card list. Click the button → platform-picker dropdown (search + 30 rows, brand-coloured glyph + name + Add CTA). Already-added platforms are filtered out with a footer note "N already added — they don't show in this list". Picking a platform creates a card.
- **Card anatomy.** Each card has 4 zones: (1) **drag handle** on the left (HTML5 drag-and-drop reorder, with `is-dragging` / `is-drag-over` visual feedback), (2) **icon-style picker** (a button that opens a 3-column grid of all variants for THAT platform — Instagram-only filtered to the 7 Instagram styles, etc.), (3) **handle input** with a fixed `@` prefix and per-platform aria-label, (4) **remove (X)** button. Cards live inside `.social-cards-list` and reorder via drag.
- **Per-platform icon-style picker.** New `.social-style-pop` popover keyed off `STYLE_PICKER_OPEN_FOR` (single-open at a time). Tile grid (3-col desktop, 2-col mobile) showing each variant's glyph at 28px above its style-name label. Selecting a tile writes `state.handles[platformId].iconStyle = '<variantId>'` (e.g. `instagram-mono-frame`). Outside-click closes the picker. Same UX skeleton as the existing `icon-picker` but scoped to the platform — no category tabs, no search.
- **Live preview.** `BLOCK_TYPES.social.preview()` rewritten to render visitor-view social icons in `state.handlesOrder` with the chosen icon style per platform. Wrapper shape (`circle` / `square` / `none`) drives the `.preview-social.shape-*` class. Filled-* and legacy variants self-provide their background, so `.shape-none` strips the wrapper for those styles to avoid double-stacking. Empty state: dashed-border "Add a social platform to see the preview" card.
- **Wrapper shape options extended** from 2 (Circle / Rounded square) to 3 — added `none` (Bare icons) so creators who pick `filled-circle` / `filled-square` icon styles don't get an extra background rectangle behind them.
- **A/B integration.** `state.handles` and `state.handlesOrder` now live in `CONTENT_FIELD_KEYS` so they route per-variant transparently. Switching to Variant B starts with empty cards (independent of A) — adding/removing/reordering platforms in B doesn't affect A. The diff card (`renderDiffCell`) accepts both legacy `{ig: '@x'}` (string handle) and the new `{ig: {handle, iconStyle}}` shape, and renders array-typed values (the order array) as `ig → tt → yt`.
- **Backward-compat migration.** `normaliseHandles(raw)` accepts both shapes — string handles get auto-upgraded to `{handle, iconStyle: <platformId>}` on first read. Pre-existing state from earlier mockup builds keeps working without manual rewrites.
- **A/B section identity chip (FIX-005, applies to all 12 block types).** New `.ab-section-chip` element sits in the Content section-head right of the H4. Two states: `data-state="available"` (neutral pill, "A/B test available") when Variant B has no diffs; `data-state="active"` (warm-tinted pill with a pulsing dot, "A/B test active · N field(s)") when B differs. Wired in `applyAbVisibility()` from `countAbDiffs()`. Goal: the user can scan the editor and instantly see "A/B testing is a feature on this block" without parsing the italic explainer line below the tabs. **NO toggle restored** — auto-detect from Variant B edits stays per DEC-093.
- **Smoke coverage.** 19/19 functional checks pass (jsdom-style harness): empty default, addSocialPlatform, updateSocialHandle (auto-prefixes `@`), pickSocialStyle, drag-reorder, removeSocialPlatform, Variant A / B independence, renderSocialVariant (brand / mono / mono-frame / outline / filled-circle / filled-square / legacy), countSocialVariants, ICON_LIBRARY untouched, empty-preview state, populated-preview shape class, diff cell with new shape, diff cell with platform array.
- **Files touched.** `app-block-editor.html` only. `creator-public.html` and the onboarding `onboarding-social.html` are NOT touched in this pass — they have their own social-stack rendering and will be reconciled in a follow-up if/when the icon-style picker ships to public pages.

### 2026-04-26 — A/B section UX overhaul: always-visible tabs + auto-detect (PR #54, FIX-001..004)

`app-block-editor.html` — second pass on the A/B implementation that landed
earlier today (commit `ccfcc0a`). User reviewed and locked DEC-093
(Option 1, auto-detect): no explicit toggle, tabs always visible,
A/B intent inferred at save from whether Variant B has differing edits.
Layout cleanup + cross-block-type consistency verification.

- **FIX-001 — Visible toggle layout.** `.section-head` gains `flex-wrap: wrap` and a `.head-spacer` filler. The `Visible` toggle stays right-aligned on wide modals and wraps to its own row on narrow widths instead of overflowing the panel.
- **FIX-002 — Variant tab labels.** Dropped the value-echo `.ab-tab-meta` suffix (`Variant A · LISTEN ON SPOTIFY`). Tabs read `[A] Variant A` and `[B] Variant B` only — no informational repeat of the field below. Tabs are now narrow enough that the section-head fits comfortably without wrapping in 99% of viewport widths.
- **FIX-003 — Always-visible tabs + auto-detect (DEC-093 = Option 1).** Removed the standalone `A/B test this block` section and its toggle. Variant A | B tabs now render at all times in the Content section header. `state.ab.enabled` is permanently `true`; the `toggleAb()` function is gone. Variant A is the primary editing surface for every tier. Variant B is fully visible and clickable for non-Business tiers but carries dimmed styling + a `🔒 Business` lock pill on the tab itself; clicking B reveals an inline `.ab-tier-callout` above the form fields explaining the gate without blurring or disabling anything (per `feedback_no_blur_premium_features`). A new `.ab-edu` line below the tabs delivers the standing pitch — "On Business, edit Variant B to test two versions… Traffic splits 50/50 and the winner promotes automatically." — visible at every tier, every block type. A/B intent auto-detects at save: `collectPremiumChanges()` adds the A/B feature only when `countAbDiffs() > 0`. The win-criteria copy ("auto-promote after 7 days or 1k clicks") moves to the `.ab-win-note` footnote that surfaces only when B has differing edits. Dual stacked preview + diff card also key off `abVariantsDiffer()` instead of the old toggle.
- **FIX-004 — 12 block-type consistency.** Manually walked the type switcher through all 12 BLOCK_TYPES (Link, Image, Embed, Heading, Divider, Social, Newsletter, Shop, Video, Accordion, Custom HTML, Countdown). Livestream removed 2026-04-26 per `feedback_no_livestream_block_in_tadaify_mvp.md` (DEC-099). Variant A | B tabs render identically positioned + sized + styled in every type. Variant B locked-state visual (dimmed + lock pill) is identical across types. The `.ab-edu` explainer is the same copy on every type (kept universal, not type-adapted — locks the brand voice). Helper bar, dual preview, diff card, copy A→B button render identically. State-routing via `getContentState()` / `writeContent()` already handles all 28 `CONTENT_FIELD_KEYS` transparently per block type, so per-type form differences route correctly into per-variant state.
- **State + lifecycle.** `loadType()` always re-seeds both variants from the freshly-defaulted state (so type-switch resets B to match A under the new type, identical to before). `saveBlock()` drops the soft-warn-on-identical-variants confirm — with auto-detect, identical = single-variant save, no friction. `Save without A/B` in the TierGate flow now collapses Variant B to match A (instead of the old "disable toggle" path).
- **Removed surface area.** Deleted standalone A/B `<section>` (`#ab-card`), the `#ab-switch` element, the `#ab-explainer` rows under the toggle, the `#ab-tier-hint` line, the `.ab-tab-meta` slots, the `updateVariantTabMeta()` function, the `toggleAb()` function, and their `window.toggleAb` export. Orphan CSS classes (`.ab-explainer*`, `.ab-pill-traffic`) kept in the stylesheet for reuse / minimal-diff.
- **New CSS primitives.** `.ab-tab.is-locked` + `.ab-tab-lock` (lock pill on Variant B tab), `.ab-edu` (italic explainer line under tabs), `.ab-tier-callout` (inline tier explainer when on Variant B + non-Business), `.ab-win-note` (footnote when B has differing edits). Tier callout uses `var(--brand-warm)` accents to align with the B variant's brand-warm pill.

### 2026-04-26 — A/B test = full Content split (PR #54 extension)

`app-block-editor.html` replaces the "Variant B label" half-feature with full
Content-section variant tabs. Every field in any block type's form (label,
url, icon, schedule, html, FAQ Q/A, social handles, autoplay, …) can now
differ between A and B — the A/B test now matches the product promise
"compare two block variants and ship the winner" (commit pending).

- **State model.** `state.ab = { enabled, activeTab, previewView, variants: { A: {...}, B: {...} } }`. Content keys (28 total per `CONTENT_FIELD_KEYS`) live at top level when A/B is off and inside `state.ab.variants[activeTab]` when A/B is on. Form rendering reads through `getContentState()`; field writes go through `writeContent()`. `BLOCK_TYPES.form()` and `.preview()` keep receiving a single content-state object — variant routing is transparent to the 12 block-type specs.
- **Variant tabs.** When A/B is on, the Content section header gains a balanced `Variant A | Variant B` tab strip (A = brand indigo pill, B = brand warm pill). Tab metadata shows a short summary of each variant's identity (label / cta / heading text — first non-empty). Switching tabs preserves state per variant; edits in A do NOT affect B.
- **Helper bar.** When the active tab is B, a dashed-warm helper bar surfaces `Copy A → B` and `Reset B to match A` actions. Reset prompts confirm if B has diverged from A.
- **Dual preview.** When A/B is on, the preview pane stacks two cards — `Variant A · 50% of visitors` + `Variant B · 50% of visitors` — both rendering live from their respective variant state. A `Both / A only / B only` picker above the preview lets the creator focus.
- **Diff summary.** A "What's different between A and B" card under the preview lists only the fields that actually differ (label, url, icon, …) — color-coded by variant. Empty values render as italic "(empty)". Object values (social handles) flatten to `ig:@x · tt:@y`. Updates live on every keystroke. When A and B are identical, the card shows "No differences yet — switch to Variant B and edit a field."
- **Toggle off.** When the user turns A/B off and B has diverged from A, a confirm prompt warns that Variant B will be discarded. On confirm, Variant A's values collapse back to the top-level state and the form continues editing as if A/B was never on.
- **Type-switch within an A/B session.** Switching block type while A/B is on re-seeds both variants from the new type's defaults (so B starts as a clone of A under the new type, not a stale clone of the previous type). The toggle stays on; the active tab resets to A.
- **Save flow.** `saveBlock()` adds: (1) soft warning when A/B is on but variants are identical (the test would be meaningless); (2) tier-gate when A/B is on but tier < Business — existing `TierGate.checkAndProceed` modal lists "A/B testing" with the count of differing fields in the meta line. `Save without` collapses A/B back to single state (keeps Variant A's values) and proceeds.
- **Tier visibility (per `feedback_no_blur_premium_features`).** The A/B section header still carries the inline `🔒 Business` tier badge. The toggle is interactive at every tier — Free/Creator/Pro users can fully explore the Variant A | B tabs, the Copy/Reset actions, and the dual preview. Only at Save time does the TierGate modal appear. Demo toolbar Free/Creator/Pro/Business switching exercises this end-to-end.
- **CSS.** New tokens-driven primitives under the existing tier-gated section block: `.ab-tabs`, `.ab-tab`, `.ab-tab-pill-a/-b`, `.ab-helper`, `.ab-explainer`, `.preview-pair`, `.ab-preview-picker`, `.ab-diff`. All colours map to `var(--brand-primary)` (A) and `var(--brand-warm)` (B). Mobile collapses the variant tab metadata + diff grid to single-column.
- **Coverage.** Functional smoke (jsdom): 30/30 checks pass — initial state, toggle on/off, variant seeding, tab switching, field isolation between variants, diff card rendering, type-switch preservation, copyAtoB, identical-variants save warning, Free-tier TierGate trigger, preview-view picker.

### 2026-04-26 — Icon picker redesigned (PR #54 extension)

`app-block-editor.html` ships a proper dropdown + search icon picker that replaces the throwaway 18-emoji grid (commit `b6bcda3`).

- **UX.** Closed state is a single trigger button showing the currently selected icon + name with a chevron. Click to open a 480px popover containing: search input (real-time filter on name + tags), 7 category tabs (Popular / Social / Music & Video / Shop & Money / Communication / Content / Generic), live count, 6-col grid (4-col on <540px). Outside-click + Esc close; Enter on the search input picks the first visible result.
- **Library.** ~126 inline SVG icons covering Lucide-style monochrome outline (94 icons inheriting `currentColor` for dark-mode safety) + Simple Icons brand logos (32 icons rendered in their official brand color: Spotify green, TikTok black, YouTube red, Stripe purple, Instagram pink, etc.). Real-impl mapping: `lucide-react` (ISC) + `simple-icons` (CC0). Mockup is dependency-free — every path inlined so the file loads over `file://`.
- **Field kind.** Added reusable `kind: 'icon-picker'` to the `BLOCK_TYPES` form spec. Multiple block types declare it and get the picker for free.
- **Block type coverage.** Picker now appears on: Link button (default `spotify`), Newsletter signup (`ctaIcon` default `send`), Product (`ctaIcon` default `shopping-cart`; renamed from "Shop product" 2026-04-26 per FIX-SHOP-001 — block now links to creator's external store, no native shop in MVP), Heading / text (optional leading `icon`), Countdown timer (label `icon` default `flame`), FAQ / accordion (optional `sectionIcon`). Image / Video / Embed / Divider / Custom HTML / Social icons row keep their existing visual treatment (Social row uses its own platform-handle picker — different concept, not conflated). Livestream block removed 2026-04-26 per `feedback_no_livestream_block_in_tadaify_mvp.md` (DEC-099).
- **Backward-compat.** State value `state.icon` (and friends) is now an icon-id string from `ICON_LIBRARY_BY_ID`. Legacy emoji values (e.g. `▶`) still render via a fallback path in `renderIconForPreview()` so any pre-existing state survives. Default for the link block changed from `▶` to `spotify`.
- **A11y + dark mode.** Trigger has `aria-haspopup="listbox"` + `aria-expanded`. Tiles have `role="option"` + `aria-selected`. Search has Escape-to-clear / Enter-to-select-first. `prefers-reduced-motion` kills the popover animation. Brand glyphs render with their fixed brand color in both light + dark mode; Lucide icons inherit the surrounding text color.

### 2026-04-26 — Tier-gating UX refactor (no-blur rule, this PR)

**Background.** During PR #54 review the user flagged that the A/B test section of `app-block-editor.html` was rendered behind a blurred overlay carrying only an "Upgrade to Business" CTA — the user couldn't read what A/B testing was, couldn't toggle anything, couldn't make an informed decision. Locked rule `feedback_no_blur_premium_features` (2026-04-26): premium UIs must be FULLY visible and interactive on every tier; gating happens at save/apply/click time, not at display time.

**Scope of refactor (PR #54 extension).**

| Mockup | Violation | Fix |
|--------|-----------|-----|
| `app-block-editor.html` | Schedule (Creator+) and A/B (Business) sections wrapped in `.locked-card` with absolute-positioned `.locked-overlay` (blur + centered upgrade card hiding form fields) | Removed `.locked-card`/`.locked-overlay` CSS + markup. Sections now `.section.tier-gated`, fully interactive on every tier. Inline `.tdf-tier-badge` in section header signals required tier. New `tier-hint` line below the inputs explains "we'll prompt to upgrade when you save". `saveBlock()` runs `TierGate.checkAndProceed` — modal lists premium features the user enabled and offers Upgrade / Save without / Cancel. |
| `app-insights.html` | 6 power-feature cards (Saved views, A/B, Digest, Replay, Identity, Parquet) used `.lc-preview` with `filter: blur(2.5px); opacity: 0.5; pointer-events: none` and a centered `.lc-cta-overlay` covering the preview | Removed the blur + opacity + pointer-events overrides on `.lc-preview`. Replaced `.lc-cta-overlay` (absolute-positioned) with `.lc-cta-footer` — a non-blurring, in-flow informational footer below the preview. Existing `.chip pro` / `.chip business` heads kept as the inline tier indicator. The faux preview content is now real-looking and fully readable on every tier. |
| `app-settings.html` | API keys tab (`#api-locked` vs `#api-unlocked`, swapped via `display:none`) and Team tab (`#team-locked` vs `#team-unlocked`) hid the entire feature UI from lower tiers | Removed the locked/unlocked div swap. Feature UIs render on every tier. New `.tier-banner` element appears above each section only when the current tier is below the requirement, explaining "click Generate / Invite — we'll prompt to upgrade". The action buttons now route through the new `gateAction()` helper which calls `TierGate.checkAndProceed`. |
| `app-block-picker.html` | Locked block types use `opacity: 0.85` on `.type-card.is-locked` + click → confirm() → upgrade flow | **No change** — current pattern keeps cards fully readable + interactive (clicking a locked card already routes to upgrade). Marginal opacity dim is informational, not obscuring. |
| `app-domain.html`, `app-dashboard.html`, `landing.html`, `pricing.html`, onboarding-* | No tier-gating violations | n/a |

**New shared components in `shared/partials.js`.**

- `TadaifyPartials.renderTierBadge(tier, opts)` — returns the canonical `<span class="tdf-tier-badge tier-...">` markup (inline pill, color-coded per tier: Creator = teal, Pro = brand-primary indigo, Business = warm amber). Accepts `{ included: boolean, tooltip: string }`.
- `TierGate.checkAndProceed({ currentTier, features, onProceed, onSaveWithout, onUpgrade, onCancel })` — save-time validation. If the user's tier covers every required tier, runs `onProceed` immediately. Otherwise opens the shared upgrade modal listing premium features + branches: Upgrade / Save without premium / Cancel.
- `TierGate.open` / `TierGate.close` — direct modal control for callers that don't want the auto-skip semantics.
- CSS for `.tdf-tier-badge` and `.tdf-gate-*` is shipped inline by partials.js; no extra CSS file required.

### 2026-04-25 — Settings mockup added (F-180..199 scope)
- `app-settings.html` NEW — full settings panel: Account, Billing, Security, GDPR & data, API keys (Pro-gated), Team (Business-gated), Danger zone
- AP-010 one-click cancel modal wired (no survey cascade)
- AP-029 "page stays live" messaging in cancel + delete account flows
- F-COMPLIANCE-001 GDPR data export + delete account + cookie preferences
- DEC-CREATOR-API-01 API keys section with MCP server install snippet + Custom GPT template
- DEC-PRICELOCK-01 "Locked for life" amber pill on Billing plan card
- DEC-PRICELOCK-02 custom domain add-on line items in Billing
- Demo toolbar (bottom-right, aria-hidden) for tier switching (Free/Creator/Pro/Business) to preview gated states
- Settings link wired in `app-dashboard.html` and `app-domain.html` sidebars (were placeholders)
- Cross-mockup link audit completed — 17 files scanned, 0 broken refs, 3 orphan candidates documented

### 2026-04-25 — Single-sidebar IA adopted (this PR)
- `app-dashboard.html` replaced with v2 content (single-sidebar + breadcrumb stepper accordion). Old dual-rail v1 superseded.
- `app-domain.html` added — full custom-domain management mockup
- All cross-mockup links wired (Domain item in dashboard sidebar → app-domain.html)
- This README created

### 2026-04-25 — Second wave DEC application
- Plagiarism renames: Header → Profile, Wallpaper → Background (Design sub-nav)
- Animations 2-section (Entrance + Ambient + Accessibility) per DEC-ANIMATIONS-SPLIT-01
- Pinned message primitive on Page tab per DEC-PINNED-MSG-01
- AI theme matcher in Theme panel (predefined catalogue + token meter, Free 5 quota)
- Universal $2 custom-domain add-on (pricing, onboarding-tier)
- Add Block modal extensible (43 integrations, 9 categories, search + URL detect)
- Light/dark mode toggle in dashboard topbar (replaces avatar)
- Footer panel: opt-in support badge default OFF (DEC-OPT-BADGE refines AP-001)

### 2026-04-26 — Block editor + picker + sidebar partial canon
- `shared/partials.js` extended with `data-partial="app-sidebar"` injector. Renders the canonical Pages-parent sidebar with `data-active` / `data-tier` / `data-handle` / `data-username` attributes. CSS shipped inline so a single `<script src="./shared/partials.js"></script>` is enough; works over `file://`.
- `app-settings.html`, `app-domain.html`, `app-insights.html` migrated to use the partial — 73 lines of duplicated sidebar markup removed per file.
- `app-dashboard.html` keeps in-page sidebar (owns the Pages/Design accordion + tab-switching state) but two cross-link inconsistencies fixed: Domain link no longer says "soon" (app-domain.html exists), Insights links to `./app-insights.html` (was an internal placeholder tab).
- `app-block-editor.html` NEW — initially shipped as a 480px right-side drawer; refactored on 2026-04-26 (same PR) to a 960px centered modal with two-column layout (form left + sticky live preview right) per `feedback_no_right_side_drawers` (user-locked rule: edit views are always centered modals, never drawers). Mobile (<720px) renders full-screen with a chevron-collapsible preview. 12 block-type forms + future-ready scaffolding (schedule visibility for Creator+, A/B for Business as locked-state UI; AI suggest sub-modal; analytics drill-down to Insights). Issue #52.
- `app-block-picker.html` NEW — modal gallery with search, category tabs, "Most clicked" badges, AI suggest hero CTA → 5 example block sets sub-modal. Issue #53.
- Sidebar consistency audit added to README (see "Sidebar consistency audit" section above).

### 2026-04-25 — Multi-page foundations
- F-MULTIPAGE-001 issue created (#8) — post-MVP Q+1
- F-ARCHITECTURE-001 spec entry — global vs per-page settings boundary
- 11 page-type stories in Backlog (#13-#23) for predefined templates

### 2026-04-25 — Auth screens refresh
- `register.html` refreshed (light/dark toggle, trust trio chip, "log in" link, AP-013 "no phone" microcopy)
- `login.html` NEW (sister page, magic-link toggle, welcome-back hint)

### 2026-04-25 — Pricing landing refresh (F-PRICING-LANDING-001)
- All locked DECs reflected: Pages tier ladder, AI quotas 5/20/100/∞, Creator API spotlight, $2 add-on banner
- Compare matrix added (7 categories × 4 tiers)

### 2026-04-24 — DEC-MULTIPAGE-01 application
- Pages tier ladder added to pricing.html, onboarding-tier.html
- Dashboard sidebar: Pages accordion (parent) with Home as first page + disabled Add page sub-item. Mirror this structure in every dashboard-side mockup; first page is always labeled **Home**, never "Homepage" (rejected 2026-04-25).
- Issues #1, #3 forward-compat updates

### 2026-04-24 — Initial dashboard mockup wave
- 10-variant exploration (`app-dashboard-variants.html`)
- V1 Classic Sidebar selected
- Polished V1 with profile card / Design sub-nav / 3-way device toggle
- Subsequent iterations added Animations sub-tab, AI matcher, theme toggle, support badge, plagiarism mitigations

### 2026-04-24 — Onboarding flow + auth + landing
- 7-screen onboarding flow created (welcome → social → template → blocks → tier → complete)
- Landing page, register page, pricing page initial versions

## Notes for implementing agents

- Read this README before starting any F-XXX-NNN implementation. The DEC trail and brand-lock are non-negotiable.
- If your implementation deviates from the mockup, add a comment to the corresponding issue (per `change-tracker` skill) BEFORE merging the PR.
- Cross-mockup navigation references: `./app-dashboard.html`, `./app-domain.html`, `./app-settings.html`, `./creator-public.html`, etc. — keep relative paths working when implementing routes.
- All mockups use `./shared/` assets — implementation should mirror this design-token approach (tokens.css → CSS variables in production).
